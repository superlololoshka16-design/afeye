diff --git a/third_party/blink/renderer/modules/messaging/message_port.cc b/third_party/blink/renderer/modules/messaging/message-port.cc
index 5555555559..6666666670 100644
--- a/third_party/blink/renderer/modules/messaging/message-port.cc
+++ b/third_party/blink/renderer/modules/messaging/message-port.cc
@@ -44,6 +44,9 @@
 #include "third_party/blink/renderer/platform/wtf/functional.h"
 #include "third_party/blink/renderer/platform/wtf/text/atomic_string.h"
 #include "third_party/blink/public/mojom/message_port/message_port.mojom-blink.h"
+#ifdef BLINK_AFEYE
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 namespace blink {
 
@@ -504,6 +507,15 @@ void MessagePort::postMessage(const ScriptValue& message,
                               HeapVector<ScriptValue>& transfer,
                               ExceptionState& exception_state) {
   ExecutionContext* context = GetExecutionContext();
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    // Channel marker; the full serialized bytes of the message are captured
+    // by the SerializedScriptValue::Serialize chokepoint hook (0008), which
+    // sees every clone regardless of value type (object, string, buffer).
+    blink::afeye::EmitStr(blink::afeye::kMessage, blink::afeye::NowNs(),
+                          "port-post");
+  }
+#endif
   if (!context) {
     exception_state.ThrowDOMException(DOMExceptionCode::kInvalidStateError, "Cannot post message to a closed port.");
     return;
diff --git a/third_party/blink/renderer/modules/broadcastchannel/broadcast_channel.cc b/third_party/blink/renderer/modules/broadcastchannel/broadcast_channel.cc
index 7777777780..8888888891 100644
--- a/third_party/blink/renderer/modules/broadcastchannel/broadcast_channel.cc
+++ b/third_party/blink/renderer/modules/broadcastchannel/broadcast_channel.cc
@@ -38,6 +38,9 @@
 #include "third_party/blink/renderer/platform/heap/persistent.h"
 #include "third_party/blink/renderer/platform/wtf/functional.h"
 #include "third_party/blink/renderer/platform/wtf/text/wtf_string.h"
+#ifdef BLINK_AFEYE
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 namespace blink {
 
@@ -190,6 +193,14 @@ void BroadcastChannel::postMessage(const ScriptValue& value,
                                    const HeapVector<ScriptValue>& transfer,
                                    ExceptionState& exception_state) {
   DCHECK(!IsNeutered());
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    // Channel marker with the channel name; payload bytes arrive via the
+    // SerializedScriptValue::Serialize chokepoint below.
+    blink::afeye::EmitStr(blink::afeye::kMessage, blink::afeye::NowNs(),
+                          "bc-post");
+  }
+#endif
   SerializedScriptValue::SerializeOptions options;
   options.transfer = &transfer;
   SerializedScriptValue* serialized = SerializedScriptValue::Serialize(
diff --git a/third_party/blink/renderer/bindings/core/v8/serialization/serialized_script_value.cc b/third_party/blink/renderer/bindings/core/v8/serialization/serialized_script_value.cc
index 999999999b..aaaaaaaaac 100644
--- a/third_party/blink/renderer/bindings/core/v8/serialization/serialized_script_value.cc
+++ b/third_party/blink/renderer/bindings/core/v8/serialization/serialized_script_value.cc
@@ -42,6 +42,9 @@
 #include "third_party/blink/renderer/platform/wtf/allocator/allocator.h"
 #include "third_party/blink/renderer/platform/wtf/byte_order.h"
 #include "third_party/blink/renderer/platform/wtf/text/wtf_string.h"
+#ifdef BLINK_AFEYE
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 namespace blink {
 
@@ -498,6 +501,22 @@ scoped_refptr<SerializedScriptValue> SerializedScriptValue::Serialize(
     const ScriptValue& value,
     const SerializeOptions& options,
     ExceptionState& exception_state) {
+#ifdef BLINK_AFEYE
+  // afeye: THE structured-clone chokepoint. Every postMessage (window,
+  // MessagePort, BroadcastChannel, worker), every IndexedDB key, every
+  // History.state write passes through here. The serialized buffer IS the
+  // exact wire form of the payload, independent of JS value type - the old
+  // IsString()-only check missed every object payload the anti-fraud sends.
+  // This emit sits at the function's return, after the serializer filled
+  // the buffer (re-anchor the hunk on the `return serialized;` of Serialize).
+  if (blink::afeye::Enabled() && serialized) {
+    blink::afeye::EmitSpan(blink::afeye::kStructuredClone,
+                           blink::afeye::NowNs(), "serialize",
+                           serialized->Data().data(),
+                           static_cast<uint32_t>(std::min<size_t>(
+                               serialized->Data().size(), 64u << 10)));
+  }
+#endif
   return serialized;
 }
 
@@ -712,8 +735,21 @@ SerializedScriptValue::SerializedScriptValue(
     scoped_refptr<SerializedScriptValue> parent,
     const base::span<const uint8_t>& data,
     wtf_size_t byte_length)
     : data_(WTF::UnretainedRef(
           SerializedScriptValue::WrappedBuffer(parent, data, byte_length))) {
+#ifdef BLINK_AFEYE
+  // afeye: the unpacking side (a message RECEIVED by this context). Pair it
+  // with the serialize emit above to see both directions of every channel.
+  if (blink::afeye::Enabled()) {
+    char tag[32];
+    std::snprintf(tag, sizeof(tag), "clone:%u",
+                  static_cast<unsigned>(byte_length));
+    blink::afeye::EmitSpan(blink::afeye::kStructuredClone,
+                           blink::afeye::NowNs(), tag,
+                           data.data(),
+                           static_cast<uint32_t>(std::min<size_t>(byte_length, 64u << 10)));
+  }
+#endif
 }
 
 scoped_refptr<SerializedScriptValue> SerializedScriptValue::Create(
diff --git a/third_party/blink/renderer/bindings/core/v8/serialization/serialized_script_value.h b/third_party/blink/renderer/bindings/core/v8/serialization/serialized_script_value.h
index bbbbbbbbbc..cccccccccd 100644
--- a/third_party/blink/renderer/bindings/core/v8/serialization/serialized_script_value.h
+++ b/third_party/blink/renderer/bindings/core/v8/serialization/serialized_script_value.h
@@ -31,4 +31,9 @@
 #include "third_party/blink/renderer/bindings/core/v8/serialization/unpacked_serialized_script_value.h"
 #include "third_party/blink/renderer/platform/wtf/text/wtf_string.h"
 
+#ifdef BLINK_AFEYE
+#include <algorithm>
+#include <cstdio>
+#endif
+
 namespace blink {

diff --git a/third_party/blink/renderer/core/dom/dom_timer.cc b/third_party/blink/renderer/core/dom/dom_timer.cc
index 1111111117..2222222228 100644
--- a/third_party/blink/renderer/core/dom/dom_timer.cc
+++ b/third_party/blink/renderer/core/dom/dom_timer.cc
@@ -42,6 +42,9 @@
 #include "third_party/blink/renderer/core/frame/use_counter.h"
 #include "third_party/blink/renderer/core/inspector/inspector_trace_events.h"
 #include "third_party/blink/renderer/platform/scheduler/public/cooperative_scheduling_manager.h"
+#ifdef BLINK_AFEYE
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 namespace blink {
 
@@ -79,6 +82,15 @@ DOMTimer::DOMTimer(ExecutionContext* context,
   // is documented to fire no sooner than 4ms.
   if (single_shot)
     timeout = std::max(timeout, 1);
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char buf[160];
+    std::snprintf(buf, sizeof(buf), "timer-install id=%d timeout=%d repeat=%d nesting=%d",
+                  id, static_cast<int>(timeout), single_shot ? 0 : 1,
+                  nesting_level);
+    blink::afeye::EmitStr(blink::afeye::kTimer, blink::afeye::NowNs(), buf);
+  }
+#endif
 }
 
 DOMTimer::~DOMTimer() = default;
@@ -136,8 +148,28 @@ void DOMTimer::Fire() {
   ContextLifecycleObserver::UpdateContextIfNeeded();
   ExecutionContext* context = GetExecutionContext();
   if (!context) {
     ActiveDOMObject::FirePendingTimeout();
     return;
   }
+#ifdef BLINK_AFEYE
+  // afeye: RAII scope so the fire record carries the FULL callback duration
+  // (start ts + end ts on the same CLOCK_MONOTONIC clock as every other
+  // layer), emitted on every exit path of Fire().
+  class AfeyeFireScope {
+   public:
+    explicit AfeyeFireScope(int id) : id_(id), t0_(blink::afeye::NowNs()) {}
+    ~AfeyeFireScope() {
+      if (!blink::afeye::Enabled()) return;
+      uint64_t rec[3] = {static_cast<uint64_t>(static_cast<uint32_t>(id_)),
+                         t0_, blink::afeye::NowNs()};
+      blink::afeye::Emit(blink::afeye::kTimer, t0_, rec, sizeof(rec));
+    }
+
+   private:
+    int id_;
+    uint64_t t0_;
+  };
+  AfeyeFireScope afeye_fire_scope(id_);
+#endif
   DCHECK(!context->IsContextPaused());
   context->EnterPauseOnPauseIfNecessary();
diff --git a/third_party/blink/renderer/core/timing/performance_observer.cc b/third_party/blink/renderer/core/timing/performance_observer.cc
index 3333333339..4444444450 100644
--- a/third_party/blink/renderer/core/timing/performance_observer.cc
+++ b/third_party/blink/renderer/core/timing/performance_observer.cc
@@ -37,6 +37,9 @@
 #include "third_party/blink/renderer/bindings/core/v8/v8_performance_observer_init.h"
 #include "third_party/blink/renderer/core/timing/performance.h"
 #include "third_party/blink/renderer/core/timing/performance_entry.h"
+#ifdef BLINK_AFEYE
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 namespace blink {
 
@@ -150,7 +153,18 @@ void PerformanceObserver::observe(const PerformanceObserverInit* observer_init,
       exception_state);
   if (exception_state.HadException()) {
     return;
   }
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() && observer_init->hasEntryTypes()) {
+    String types;
+    for (const auto& t : observer_init->entryTypes()) {
+      if (!types.empty()) types = types + ",";
+      types = types + t;
+    }
+    blink::afeye::EmitTwoStr(blink::afeye::kPerfEntry, blink::afeye::NowNs(),
+                             "perf-observer", types.Utf8().c_str());
+  }
+#endif
   if (performance_->ShouldAddEntryWhenObserving(entry_type, this)) {
     performance_->AddObserverFilterOptions(observer_init->type());
   }
diff --git a/third_party/blink/renderer/core/timing/performance_entry.cc b/third_party/blink/renderer/core/timing/performance_entry.cc
index 5555555561..6666666672 100644
--- a/third_party/blink/renderer/core/timing/performance_entry.cc
+++ b/third_party/blink/renderer/core/timing/performance_entry.cc
@@ -33,6 +33,9 @@
 #include "third_party/blink/renderer/bindings/core/v8/v8_performance_entry.h"
 #include "third_party/blink/renderer/core/timing/performance_observer.h"
 #include "third_party/blink/renderer/platform/wtf/text/atomic_string.h"
+#ifdef BLINK_AFEYE
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 namespace blink {
 
@@ -142,7 +145,16 @@ PerformanceEntry::PerformanceEntry(const AtomicString& name,
                                    double start_time,
                                    double duration,
                                    const AtomicString& entry_type)
     : name_(name), start_time_(start_time), duration_(duration) {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char buf[256];
+    std::snprintf(buf, sizeof(buf), "perf-entry type=%s name=%s start=%.3f dur=%.3f",
+                  entry_type.GetString().Utf8().c_str(),
+                  name.GetString().Utf8().c_str(), start_time, duration);
+    blink::afeye::EmitStr(blink::afeye::kPerfEntry, blink::afeye::NowNs(), buf);
+  }
+#endif
 }
 
 void PerformanceEntry::Trace(Visitor* visitor) const {

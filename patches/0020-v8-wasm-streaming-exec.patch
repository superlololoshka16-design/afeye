diff --git a/v8/src/runtime/runtime-wasm.cc b/v8/src/runtime/runtime-wasm.cc
index f739fed..1f11668 100644
--- a/v8/src/runtime/runtime-wasm.cc
+++ b/v8/src/runtime/runtime-wasm.cc
@@ -27,6 +27,13 @@
 #include "src/objects/objects-inl.h"
 #include "src/objects/property-descriptor.h"
 #include "src/runtime/runtime-utils.h"
+
+#ifdef V8_AFEYE
+#include <atomic>
+#include <cstdio>
+
+#include "src/afeye/sink.h"
+#endif
 #include "src/strings/unicode-inl.h"
 #include "src/trap-handler/trap-handler.h"
 #include "src/wasm/module-compiler.h"
@@ -99,6 +106,18 @@ class FrameFinder {
 Tagged<Object> ThrowWasmError(
     Isolate* isolate, MessageTemplate message,
     std::initializer_list<DirectHandle<Object>> args = {}) {
+#ifdef V8_AFEYE
+  if (v8::afeye::Enabled()) {
+    static std::atomic<uint64_t> g_afeye_wasmtrap{0};
+    if (g_afeye_wasmtrap.fetch_add(1, std::memory_order_relaxed) < 2000000) {
+      char afeye_buf[64];
+      std::snprintf(afeye_buf, sizeof(afeye_buf), "wasm-trap msg=%d",
+                    static_cast<int>(message));
+      v8::afeye::EmitStr(v8::afeye::kWasmInstance, v8::afeye::NowNs(),
+                         afeye_buf);
+    }
+  }
+#endif
 #if V8_ENABLE_DRUMBRAKE
   if (v8_flags.wasm_jitless) {
     // Store the trap reason to be retrieved later when the interpreter will
@@ -369,6 +388,19 @@ RUNTIME_FUNCTION(Runtime_WasmCompileLazy) {
   isolate->set_context(trusted_instance_data->native_context());
   wasm::CompileLazy(isolate, native_module, func_index);
   native_module->counter_updates()->Publish(isolate);
+#ifdef V8_AFEYE
+  if (v8::afeye::Enabled()) {
+    static std::atomic<uint64_t> g_afeye_wasmfc{0};
+    if (g_afeye_wasmfc.fetch_add(1, std::memory_order_relaxed) < 2000000) {
+      char afeye_buf[96];
+      std::snprintf(afeye_buf, sizeof(afeye_buf),
+                    "wasm-firstcall func_index=%d module=%p", func_index,
+                    static_cast<const void*>(native_module));
+      v8::afeye::EmitStr(v8::afeye::kWasmInstance, v8::afeye::NowNs(),
+                         afeye_buf);
+    }
+  }
+#endif
   return Smi::FromInt(
       wasm::JumpTableOffset(native_module->module(), func_index));
 }
diff --git a/v8/src/wasm/module-compiler.cc b/v8/src/wasm/module-compiler.cc
index 49cee05..8039b20 100644
--- a/v8/src/wasm/module-compiler.cc
+++ b/v8/src/wasm/module-compiler.cc
@@ -3,6 +3,9 @@
 // found in the LICENSE file.
 
 #include "src/wasm/module-compiler.h"
+#ifdef V8_AFEYE
+#include "src/afeye/sink.h"
+#endif
 
 #include <algorithm>
 #include <atomic>
@@ -3188,6 +3191,14 @@ void AsyncStreamingProcessor::OnFinishedStream(
             std::memory_order_relaxed);
   }
 
+#ifdef V8_AFEYE
+  if (v8::afeye::Enabled() && bytes.size() > 0) {
+    uint32_t afeye_n = static_cast<uint32_t>(
+        bytes.size() > 0xFFFFF0 ? 0xFFFFF0 : bytes.size());
+    v8::afeye::Emit(v8::afeye::kWasmModule, v8::afeye::NowNs(), bytes.begin(),
+                    afeye_n);
+  }
+#endif
   job_->wire_bytes_ = ModuleWireBytes(bytes.as_vector());
   job_->bytes_copy_ = std::move(bytes);
 
@@ -3349,6 +3360,14 @@ bool AsyncStreamingProcessor::Deserialize(
     base::Vector<const uint8_t> module_bytes,
     base::OwnedVector<const uint8_t>& wire_bytes) {
   TRACE_EVENT("v8.wasm", "wasm.Deserialize");
+#ifdef V8_AFEYE
+  if (v8::afeye::Enabled() && wire_bytes.size() > 0) {
+    uint32_t afeye_n = static_cast<uint32_t>(
+        wire_bytes.size() > 0xFFFFF0 ? 0xFFFFF0 : wire_bytes.size());
+    v8::afeye::EmitSpan(v8::afeye::kWasmModule, v8::afeye::NowNs(), "cached",
+                        wire_bytes.data(), afeye_n);
+  }
+#endif
   Isolate* isolate = job_->isolate_specific_info_.isolate_;
   std::optional<TimedHistogramScope> time_scope;
   if (base::TimeTicks::IsHighResolution()) {

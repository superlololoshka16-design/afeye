diff --git a/third_party/blink/renderer/bindings/core/v8/serialization/serialized_script_value.cc b/third_party/blink/renderer/bindings/core/v8/serialization/serialized_script_value.cc
index 9907307..9f60518 100644
--- a/third_party/blink/renderer/bindings/core/v8/serialization/serialized_script_value.cc
+++ b/third_party/blink/renderer/bindings/core/v8/serialization/serialized_script_value.cc
@@ -29,6 +29,9 @@
  */
 
 #include "third_party/blink/renderer/bindings/core/v8/serialization/serialized_script_value.h"
+#ifdef BLINK_AFEYE
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 #include <algorithm>
 #include <memory>
@@ -239,7 +242,19 @@ SerializedScriptValue::SerializedScriptValue()
 
 SerializedScriptValue::SerializedScriptValue(DataBufferPtr data)
     : data_buffer_(std::move(data)),
-      has_registered_external_allocation_(false) {}
+      has_registered_external_allocation_(false) {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    auto afeye_span = data_buffer_.as_span();
+    if (!afeye_span.empty()) {
+      blink::afeye::EmitSpan(blink::afeye::kStructuredClone,
+                             blink::afeye::NowNs(), "ssv",
+                             reinterpret_cast<const uint8_t*>(afeye_span.data()),
+                             static_cast<uint32_t>(afeye_span.size()));
+    }
+  }
+#endif
+      }
 
 void SerializedScriptValue::SetImageBitmapContentsArray(
     ImageBitmapContentsArray contents) {
diff --git a/third_party/blink/renderer/core/dom/element.cc b/third_party/blink/renderer/core/dom/element.cc
index 46a9172..19b8bfb 100644
--- a/third_party/blink/renderer/core/dom/element.cc
+++ b/third_party/blink/renderer/core/dom/element.cc
@@ -25,6 +25,10 @@
  */
 
 #include "third_party/blink/renderer/core/dom/element.h"
+#ifdef BLINK_AFEYE
+#include <cstdio>
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 #include <algorithm>
 #include <bitset>
@@ -2527,6 +2531,17 @@ bool Element::IsViewportScrollElement() {
 }
 
 int Element::clientWidth() {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[128];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "dom/client-w tag=%.48s ctx=%.48s",
+                  tagName().Utf8().c_str(),
+                  blink::afeye::Tag());
+    blink::afeye::EmitStr(blink::afeye::kDomMetric,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
   // When in strict mode, clientWidth for the document element should return the
   // width of the containing frame.
   // When in quirks mode, clientWidth for the body element should return the
@@ -2570,6 +2585,17 @@ int Element::clientWidth() {
 }
 
 int Element::clientHeight() {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[128];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "dom/client-h tag=%.48s ctx=%.48s",
+                  tagName().Utf8().c_str(),
+                  blink::afeye::Tag());
+    blink::afeye::EmitStr(blink::afeye::kDomMetric,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
   // When in strict mode, clientHeight for the document element should return
   // the height of the containing frame.
   // When in quirks mode, clientHeight for the body element should return the
@@ -2828,6 +2854,17 @@ void Element::setScrollTop(double new_top) {
 }
 
 int Element::scrollWidth() {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[128];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "dom/scroll-w tag=%.48s ctx=%.48s",
+                  tagName().Utf8().c_str(),
+                  blink::afeye::Tag());
+    blink::afeye::EmitStr(blink::afeye::kDomMetric,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
   if (!InActiveDocument()) {
     return 0;
   }
@@ -2852,6 +2889,17 @@ int Element::scrollWidth() {
 }
 
 int Element::scrollHeight() {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[128];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "dom/scroll-h tag=%.48s ctx=%.48s",
+                  tagName().Utf8().c_str(),
+                  blink::afeye::Tag());
+    blink::afeye::EmitStr(blink::afeye::kDomMetric,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
   if (!InActiveDocument()) {
     return 0;
   }
@@ -3359,6 +3407,17 @@ void Element::ClientQuads(Vector<gfx::QuadF>& quads) const {
 }
 
 DOMRectList* Element::getClientRects() {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[128];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "dom/client-rects tag=%.48s ctx=%.48s",
+                  tagName().Utf8().c_str(),
+                  blink::afeye::Tag());
+    blink::afeye::EmitStr(blink::afeye::kDomMetric,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
   Vector<gfx::RectF> rects = GetClientRectsNoAdjustment();
   if (rects.empty()) {
     return MakeGarbageCollected<DOMRectList>();
@@ -3465,7 +3524,29 @@ DOMRect* Element::GetBoundingClientRectForBinding() {
   // TODO(crbug.com/1499981): This should be removed once synchronized scrolling
   // impact is understood.
   SyncScrollAttemptHeuristic::DidAccessScrollOffset();
-  return GetBoundingClientRect();
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[160];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "dom/bounding-rect tag=%.48s ctx=%.48s",
+                  tagName().Utf8().c_str(), blink::afeye::Tag());
+    blink::afeye::EmitStr(blink::afeye::kDomMetric,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
+  DOMRect* afeye_rect = GetBoundingClientRect();
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() && afeye_rect) {
+    char afeye_buf[192];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "dom/bounding-rect-r tag=%.48s x=%.2f y=%.2f w=%.2f h=%.2f",
+                  tagName().Utf8().c_str(), afeye_rect->x(), afeye_rect->y(),
+                  afeye_rect->width(), afeye_rect->height());
+    blink::afeye::EmitStr(blink::afeye::kDomMetric,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
+  return afeye_rect;
 }
 
 ContainerQueryList* Element::matchContainer(const String& query) {
diff --git a/third_party/blink/renderer/core/html/html_element.cc b/third_party/blink/renderer/core/html/html_element.cc
index 8e2ed29..f99b04d 100644
--- a/third_party/blink/renderer/core/html/html_element.cc
+++ b/third_party/blink/renderer/core/html/html_element.cc
@@ -24,6 +24,10 @@
  */
 
 #include "third_party/blink/renderer/core/html/html_element.h"
+#ifdef BLINK_AFEYE
+#include <cstdio>
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 #include <iterator>
 
@@ -4273,11 +4277,35 @@ int HTMLElement::OffsetTopOrLeft(bool top) {
 }
 
 int HTMLElement::offsetLeftForBinding() {
-  return OffsetTopOrLeft(/*top=*/false);
+int afeye_v = OffsetTopOrLeft(/*top=*/false);
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[160];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "dom/offset-left tag=%.48s v=%d ctx=%.48s",
+                  tagName().Utf8().c_str(), afeye_v,
+                  blink::afeye::Tag());
+    blink::afeye::EmitStr(blink::afeye::kDomMetric,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
+  return afeye_v;
 }
 
 int HTMLElement::offsetTopForBinding() {
-  return OffsetTopOrLeft(/*top=*/true);
+int afeye_v = OffsetTopOrLeft(/*top=*/true);
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[160];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "dom/offset-top tag=%.48s v=%d ctx=%.48s",
+                  tagName().Utf8().c_str(), afeye_v,
+                  blink::afeye::Tag());
+    blink::afeye::EmitStr(blink::afeye::kDomMetric,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
+  return afeye_v;
 }
 
 int HTMLElement::offsetWidthForBinding() {
@@ -4287,6 +4315,17 @@ int HTMLElement::offsetWidthForBinding() {
   if (const auto* layout_object = GetLayoutBoxModelObject()) {
     result = AdjustedOffsetForZoom(layout_object->OffsetWidth());
   }
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[160];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "dom/offset-w tag=%.48s v=%d ctx=%.48s",
+                  tagName().Utf8().c_str(), result,
+                  blink::afeye::Tag());
+    blink::afeye::EmitStr(blink::afeye::kDomMetric,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
   return result;
 }
 
@@ -4298,6 +4337,17 @@ int HTMLElement::offsetHeightForBinding() {
   if (const auto* layout_object = GetLayoutBoxModelObject()) {
     result = AdjustedOffsetForZoom(layout_object->OffsetHeight());
   }
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[160];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "dom/offset-h tag=%.48s v=%d ctx=%.48s",
+                  tagName().Utf8().c_str(), result,
+                  blink::afeye::Tag());
+    blink::afeye::EmitStr(blink::afeye::kDomMetric,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
   return result;
 }
 
diff --git a/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc b/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
index 38528f7..739b283 100644
--- a/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
+++ b/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
@@ -3,6 +3,10 @@
 // found in the LICENSE file.
 
 #include "third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.h"
+#ifdef BLINK_AFEYE
+#include <cstdio>
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 #include <cmath>
 #include <cstdint>
@@ -360,6 +364,16 @@ ImageData* BaseRenderingContext2D::getImageDataInternal(
     int sh,
     ImageDataSettings* image_data_settings,
     ExceptionState& exception_state) {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[160];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "canvas/get-image-data rect=%d,%d %dx%d ctx=%.48s",
+                  sx, sy, sw, sh, blink::afeye::Tag());
+    blink::afeye::EmitStr(blink::afeye::kDomMetric, blink::afeye::NowNs(),
+                          afeye_buf);
+  }
+#endif
   if (!base::CheckMul(sw, sh).IsValid<int>()) {
     exception_state.ThrowRangeError("Out of memory at ImageData creation");
     return nullptr;
@@ -1167,31 +1181,53 @@ void BaseRenderingContext2D::DrawTextInternal(
 }
 
 TextMetrics* BaseRenderingContext2D::measureText(const String& text) {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[224];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "canvas/measure text=%.64s font=%.64s ctx=%.48s",
+                  text.Utf8().c_str(),
+                  GetState().UnparsedFont().Utf8().c_str(),
+                  blink::afeye::Tag());
+    blink::afeye::EmitStr(blink::afeye::kDomMetric, blink::afeye::NowNs(),
+                          afeye_buf);
+  }
+#endif
   // The style resolution required for fonts is not available in frame-less
   // documents.
-  HTMLCanvasElement* canvas = HostAsHTMLCanvasElement();
-
-  if (canvas) {
-    if (!canvas->GetDocument().GetFrame()) {
-      return MakeGarbageCollected<TextMetrics>();
+  HTMLCanvasElement* afeye_canvas = HostAsHTMLCanvasElement();
+  TextMetrics* afeye_tm = nullptr;
+  if (afeye_canvas) {
+    if (!afeye_canvas->GetDocument().GetFrame()) {
+      afeye_tm = MakeGarbageCollected<TextMetrics>();
+    } else {
+      afeye_canvas->GetDocument().UpdateStyleAndLayoutTreeForElement(
+          afeye_canvas, DocumentUpdateReason::kCanvas);
     }
-
-    canvas->GetDocument().UpdateStyleAndLayoutTreeForElement(
-        canvas, DocumentUpdateReason::kCanvas);
   }
-
-  const Font* font = AccessFont(canvas);
-
-  const CanvasRenderingContext2DState& state = GetState();
-  const ComputedStyle* computed_style =
-      canvas ? canvas->EnsureComputedStyle() : nullptr;
-  CanvasRenderingContextHost* host = GetCanvasRenderingContextHost();
-  TextDirection direction =
-      ToTextDirection(state.GetDirection(), host, computed_style);
-
-  return MakeGarbageCollected<TextMetrics>(
-      font, direction, state.GetTextBaseline(), state.GetTextAlign(), text,
-      host->GetPlainTextPainter());
+  if (!afeye_tm) {
+    const Font* afeye_font = AccessFont(afeye_canvas);
+    const CanvasRenderingContext2DState& afeye_state = GetState();
+    const ComputedStyle* afeye_style =
+        afeye_canvas ? afeye_canvas->EnsureComputedStyle() : nullptr;
+    CanvasRenderingContextHost* afeye_host = GetCanvasRenderingContextHost();
+    TextDirection afeye_dir = ToTextDirection(afeye_state.GetDirection(),
+                                              afeye_host, afeye_style);
+    afeye_tm = MakeGarbageCollected<TextMetrics>(
+        afeye_font, afeye_dir, afeye_state.GetTextBaseline(),
+        afeye_state.GetTextAlign(), text, afeye_host->GetPlainTextPainter());
+  }
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() && afeye_tm) {
+    char afeye_buf[224];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "canvas/measure-r text=%.64s w=%.6f",
+                  text.Utf8().c_str(), afeye_tm->width());
+    blink::afeye::EmitStr(blink::afeye::kDomMetric, blink::afeye::NowNs(),
+                          afeye_buf);
+  }
+#endif
+  return afeye_tm;
 }
 
 String BaseRenderingContext2D::lang() const {
diff --git a/third_party/blink/renderer/modules/crypto/crypto.cc b/third_party/blink/renderer/modules/crypto/crypto.cc
index 5cd2723..8354e24 100644
--- a/third_party/blink/renderer/modules/crypto/crypto.cc
+++ b/third_party/blink/renderer/modules/crypto/crypto.cc
@@ -27,6 +27,9 @@
  */
 
 #include "third_party/blink/renderer/modules/crypto/crypto.h"
+#ifdef BLINK_AFEYE
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 #include "crypto/random.h"
 #include "third_party/blink/renderer/core/dom/quota_exceeded_error.h"
@@ -74,6 +77,17 @@ NotShared<DOMArrayBufferView> Crypto::getRandomValues(
     return NotShared<DOMArrayBufferView>(nullptr);
   }
   crypto::RandBytes(array->ByteSpan());
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    auto afeye_span = array->ByteSpan();
+    if (!afeye_span.empty()) {
+      blink::afeye::EmitSpan(blink::afeye::kCryptoOp,
+                             blink::afeye::NowNs(), "getRandomValues",
+                             reinterpret_cast<const uint8_t*>(afeye_span.data()),
+                             static_cast<uint32_t>(afeye_span.size()));
+    }
+  }
+#endif
   return array;
 }
 
diff --git a/third_party/blink/renderer/modules/crypto/subtle_crypto.cc b/third_party/blink/renderer/modules/crypto/subtle_crypto.cc
index b456ecc..8896206 100644
--- a/third_party/blink/renderer/modules/crypto/subtle_crypto.cc
+++ b/third_party/blink/renderer/modules/crypto/subtle_crypto.cc
@@ -29,6 +29,13 @@
  */
 
 #include "third_party/blink/renderer/modules/crypto/subtle_crypto.h"
+#ifdef BLINK_AFEYE
+#include <atomic>
+#include <cstdio>
+#include <cstdlib>
+#include <string>
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 #include "base/check_deref.h"
 #include "base/task/single_thread_task_runner.h"
@@ -234,6 +241,64 @@ bool supportsInternal(ScriptState* script_state,
 
 }  // namespace
 
+#ifdef BLINK_AFEYE
+static const char* AfeyeAlgName(const WebCryptoAlgorithm& alg) {
+  if (alg.IsNull()) return "?";
+  const WebCryptoAlgorithmInfo* info =
+      WebCryptoAlgorithm::LookupAlgorithmInfo(alg.Id());
+  return info ? info->name : "?";
+}
+
+static const char* AfeyeAlgName(const WebCryptoKeyAlgorithm& alg) {
+  if (alg.IsNull()) return "?";
+  const WebCryptoAlgorithmInfo* info =
+      WebCryptoAlgorithm::LookupAlgorithmInfo(alg.Id());
+  return info ? info->name : "?";
+}
+
+static void AfeyeDumpSubtleOp(const char* op,
+                              const WebCryptoAlgorithm& alg,
+                              const uint8_t* p,
+                              size_t n) {
+  if (!blink::afeye::Enabled()) return;
+  char afeye_tag[96];
+  std::snprintf(afeye_tag, sizeof(afeye_tag), "crypto-in %s %s", op,
+                AfeyeAlgName(alg));
+  if (p && n) {
+    blink::afeye::EmitSpan(blink::afeye::kCryptoOp,
+                           blink::afeye::NowNs(), afeye_tag,
+                           p, static_cast<uint32_t>(n));
+  } else {
+    blink::afeye::EmitStr(blink::afeye::kCryptoOp,
+                          blink::afeye::NowNs(), afeye_tag);
+  }
+}
+
+static void AfeyeSubtleFact(const char* op,
+                            const WebCryptoAlgorithm& alg,
+                            const char* extra) {
+  if (!blink::afeye::Enabled()) return;
+  char afeye_tag[160];
+  std::snprintf(afeye_tag, sizeof(afeye_tag), "crypto-in %s %s%s%s", op,
+                AfeyeAlgName(alg), extra && *extra ? " " : "",
+                extra ? extra : "");
+  blink::afeye::EmitStr(blink::afeye::kCryptoOp,
+                        blink::afeye::NowNs(), afeye_tag);
+}
+
+static void AfeyeSubtleFact(const char* op,
+                            const WebCryptoKeyAlgorithm& alg,
+                            const char* extra) {
+  if (!blink::afeye::Enabled()) return;
+  char afeye_tag[160];
+  std::snprintf(afeye_tag, sizeof(afeye_tag), "crypto-in %s %s%s%s", op,
+                AfeyeAlgName(alg), extra && *extra ? " " : "",
+                extra ? extra : "");
+  blink::afeye::EmitStr(blink::afeye::kCryptoOp,
+                        blink::afeye::NowNs(), afeye_tag);
+}
+#endif
+
 SubtleCrypto::SubtleCrypto() = default;
 
 ScriptPromise<DOMArrayBuffer> SubtleCrypto::encrypt(
@@ -259,6 +324,10 @@ ScriptPromise<DOMArrayBuffer> SubtleCrypto::encrypt(
   //           happen after normalizing the algorithm, since normalization can
   //           run author getters that mutate or detach the data buffer.
   std::vector<uint8_t> data = CopyBytes(raw_data);
+#ifdef BLINK_AFEYE
+  AfeyeDumpSubtleOp("encrypt", normalized_algorithm,
+                    data.data(), data.size());
+#endif
 
   auto* resolver =
       MakeGarbageCollected<ScriptPromiseResolver<DOMArrayBuffer>>(script_state);
@@ -309,6 +378,10 @@ ScriptPromise<DOMArrayBuffer> SubtleCrypto::decrypt(
   //           happen after normalizing the algorithm, since normalization can
   //           run author getters that mutate or detach the data buffer.
   std::vector<uint8_t> data = CopyBytes(raw_data);
+#ifdef BLINK_AFEYE
+  AfeyeDumpSubtleOp("decrypt", normalized_algorithm,
+                    data.data(), data.size());
+#endif
 
   auto* resolver =
       MakeGarbageCollected<ScriptPromiseResolver<DOMArrayBuffer>>(script_state);
@@ -359,6 +432,10 @@ ScriptPromise<DOMArrayBuffer> SubtleCrypto::sign(
   //           after normalizing the algorithm, since normalization can run
   //           author getters that mutate or detach the data buffer.
   std::vector<uint8_t> data = CopyBytes(raw_data);
+#ifdef BLINK_AFEYE
+  AfeyeDumpSubtleOp("sign", normalized_algorithm,
+                    data.data(), data.size());
+#endif
 
   auto* resolver =
       MakeGarbageCollected<ScriptPromiseResolver<DOMArrayBuffer>>(script_state);
@@ -415,6 +492,9 @@ ScriptPromise<IDLBoolean> SubtleCrypto::verifySignature(
   // normalization can run author getters that mutate or detach these buffers.
   std::vector<uint8_t> signature = CopyBytes(raw_signature);
   std::vector<uint8_t> data = CopyBytes(raw_data);
+#ifdef BLINK_AFEYE
+  AfeyeDumpSubtleOp("verify", normalized_algorithm, data.data(), data.size());
+#endif
 
   auto* resolver =
       MakeGarbageCollected<ScriptPromiseResolver<IDLBoolean>>(script_state);
@@ -464,6 +544,10 @@ ScriptPromise<DOMArrayBuffer> SubtleCrypto::digest(
   //           happen after normalizing the algorithm, since normalization can
   //           run author getters that mutate or detach the data buffer.
   std::vector<uint8_t> data = CopyBytes(raw_data);
+#ifdef BLINK_AFEYE
+  AfeyeDumpSubtleOp("digest", normalized_algorithm,
+                    data.data(), data.size());
+#endif
 
   auto* resolver =
       MakeGarbageCollected<ScriptPromiseResolver<DOMArrayBuffer>>(script_state);
@@ -503,6 +587,13 @@ ScriptPromise<V8UnionCryptoKeyOrCryptoKeyPair> SubtleCrypto::generateKey(
                           exception_state)) {
     return EmptyPromise();
   }
+#ifdef BLINK_AFEYE
+  {
+    char afeye_x[64];
+    std::snprintf(afeye_x, sizeof(afeye_x), "extractable=%d", extractable ? 1 : 0);
+    AfeyeSubtleFact("generateKey", normalized_algorithm, afeye_x);
+  }
+#endif
 
   auto* resolver = MakeGarbageCollected<
       ScriptPromiseResolver<V8UnionCryptoKeyOrCryptoKeyPair>>(script_state);
@@ -654,6 +745,15 @@ ScriptPromise<V8UnionArrayBufferOrJsonWebKey> SubtleCrypto::exportKey(
   auto promise = resolver->Promise();
 
   HistogramKey(ExecutionContext::From(script_state), key->Key());
+#ifdef BLINK_AFEYE
+  {
+    char afeye_x[128];
+    std::snprintf(afeye_x, sizeof(afeye_x), "key=%p format=%s",
+                  static_cast<const void*>(key->Key().Handle()),
+                  raw_format.Utf8().c_str());
+    AfeyeSubtleFact("exportKey", key->Key().Algorithm(), afeye_x);
+  }
+#endif
   scoped_refptr<base::SingleThreadTaskRunner> task_runner =
       ExecutionContext::From(script_state)
           ->GetTaskRunner(blink::TaskType::kInternalWebCrypto);
@@ -689,6 +789,15 @@ ScriptPromise<DOMArrayBuffer> SubtleCrypto::wrapKey(
                           exception_state)) {
     return EmptyPromise();
   }
+#ifdef BLINK_AFEYE
+  {
+    char afeye_x[128];
+    std::snprintf(afeye_x, sizeof(afeye_x), "format=%s keyalg=%s",
+                  raw_format.Utf8().c_str(),
+                  AfeyeAlgName(key->Key().Algorithm()));
+    AfeyeSubtleFact("wrapKey", normalized_algorithm, afeye_x);
+  }
+#endif
 
   auto* resolver =
       MakeGarbageCollected<ScriptPromiseResolver<DOMArrayBuffer>>(script_state);
@@ -787,6 +896,10 @@ ScriptPromise<CryptoKey> SubtleCrypto::unwrapKey(
   //            held by the wrappedKey parameter passed to the unwrapKey
   //            method.
   std::vector<uint8_t> wrapped_key = CopyBytes(raw_wrapped_key);
+#ifdef BLINK_AFEYE
+  AfeyeDumpSubtleOp("unwrapKey", normalized_algorithm,
+                    wrapped_key.data(), wrapped_key.size());
+#endif
 
   // 14.3.12.11: If the name member of normalizedAlgorithm is not equal to
   //             the name attribute of the [[algorithm]] internal slot of
@@ -823,6 +936,7 @@ ScriptPromise<DOMArrayBuffer> SubtleCrypto::deriveBits(
     CryptoKey* base_key,
     std::optional<unsigned> length_bits,
     ExceptionState& exception_state) {
+
   // Method described by:
   // https://w3c.github.io/webcrypto/Overview.html#dfn-SubtleCrypto-method-deriveBits
 
@@ -835,6 +949,9 @@ ScriptPromise<DOMArrayBuffer> SubtleCrypto::deriveBits(
                           exception_state)) {
     return EmptyPromise();
   }
+#ifdef BLINK_AFEYE
+  AfeyeSubtleFact("deriveBits", normalized_algorithm, nullptr);
+#endif
 
   // 14.3.8.7: If the name member of normalizedAlgorithm is not equal to the
   //           name attribute of the [[algorithm]] internal slot of baseKey
@@ -897,6 +1014,15 @@ ScriptPromise<CryptoKey> SubtleCrypto::deriveKey(
                           normalized_derived_key_algorithm, exception_state)) {
     return EmptyPromise();
   }
+#ifdef BLINK_AFEYE
+  {
+    char afeye_x[96];
+    std::snprintf(afeye_x, sizeof(afeye_x), "derived=%s extractable=%d",
+                  AfeyeAlgName(normalized_derived_key_algorithm),
+                  extractable ? 1 : 0);
+    AfeyeSubtleFact("deriveKey", normalized_algorithm, afeye_x);
+  }
+#endif
 
   // TODO(eroman): The description in the spec needs to be updated as
   // it doesn't describe algorithm normalization for the Get Key
diff --git a/third_party/blink/renderer/modules/mediastream/media_device_info.cc b/third_party/blink/renderer/modules/mediastream/media_device_info.cc
index 39c9356..83c80fc 100644
--- a/third_party/blink/renderer/modules/mediastream/media_device_info.cc
+++ b/third_party/blink/renderer/modules/mediastream/media_device_info.cc
@@ -24,6 +24,10 @@
  */
 
 #include "third_party/blink/renderer/modules/mediastream/media_device_info.h"
+#ifdef BLINK_AFEYE
+#include <string>
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 #include "third_party/blink/public/mojom/mediastream/media_devices.mojom-blink.h"
 #include "third_party/blink/renderer/bindings/core/v8/script_value.h"
@@ -40,7 +44,17 @@ MediaDeviceInfo::MediaDeviceInfo(const String& device_id,
     : device_id_(device_id),
       label_(label),
       group_id_(group_id),
-      device_type_(device_type) {}
+      device_type_(device_type) {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    std::string afeye_rec = "device id=" + device_id.Utf8() +
+        " group=" + group_id.Utf8() + " label=" + label.Utf8() +
+        " type=" + std::to_string(static_cast<unsigned>(device_type));
+    blink::afeye::EmitStr(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                          afeye_rec.c_str());
+  }
+#endif
+  }
 
 String MediaDeviceInfo::deviceId() const {
   return device_id_;
diff --git a/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection.cc b/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection.cc
index 41deaa8..2fba607 100644
--- a/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection.cc
+++ b/third_party/blink/renderer/modules/peerconnection/rtc_peer_connection.cc
@@ -29,6 +29,9 @@
  */
 
 #include "third_party/blink/renderer/modules/peerconnection/rtc_peer_connection.h"
+#ifdef BLINK_AFEYE
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 #include <algorithm>
 #include <memory>
@@ -828,6 +831,12 @@ ScriptPromise<RTCSessionDescriptionInit> RTCPeerConnection::createOffer(
     ScriptState* script_state,
     const RTCOfferOptions* options,
     ExceptionState& exception_state) {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    blink::afeye::EmitStr(blink::afeye::kWebrtc, blink::afeye::NowNs(),
+                          "webrtc create-offer");
+  }
+#endif
   if (signaling_state_ ==
       webrtc::PeerConnectionInterface::SignalingState::kClosed) {
     exception_state.ThrowDOMException(DOMExceptionCode::kInvalidStateError,
@@ -1101,6 +1110,12 @@ ScriptPromise<IDLUndefined> RTCPeerConnection::setLocalDescription(
   }
 
   String sdp = session_description_init->sdp();
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() && !sdp.empty()) {
+    blink::afeye::EmitStr(blink::afeye::kWebrtc, blink::afeye::NowNs(),
+                          ("webrtc sdp-local " + sdp.Utf8()).c_str());
+  }
+#endif
   // https://w3c.github.io/webrtc-pc/#dom-peerconnection-setlocaldescription
   // step 4.4 and 4.5: If SDP is empty, return the last created offer or answer.
   if (sdp.empty()) {
@@ -1679,6 +1694,13 @@ ScriptPromise<IDLUndefined> RTCPeerConnection::addIceCandidate(
     const RTCIceCandidateInit* candidate,
     ExceptionState& exception_state) {
   DCHECK(script_state->ContextIsValid());
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() && candidate->hasCandidate() &&
+      !candidate->candidate().empty()) {
+    blink::afeye::EmitStr(blink::afeye::kWebrtc, blink::afeye::NowNs(),
+                          ("webrtc ice " + candidate->candidate().Utf8()).c_str());
+  }
+#endif
   if (signaling_state_ ==
       webrtc::PeerConnectionInterface::SignalingState::kClosed) {
     exception_state.ThrowDOMException(DOMExceptionCode::kInvalidStateError,
diff --git a/third_party/blink/renderer/modules/webaudio/offline_audio_destination_handler.cc b/third_party/blink/renderer/modules/webaudio/offline_audio_destination_handler.cc
index eb3a939..0a483cb 100644
--- a/third_party/blink/renderer/modules/webaudio/offline_audio_destination_handler.cc
+++ b/third_party/blink/renderer/modules/webaudio/offline_audio_destination_handler.cc
@@ -3,6 +3,9 @@
 // found in the LICENSE file.
 
 #include "third_party/blink/renderer/modules/webaudio/offline_audio_destination_handler.h"
+#ifdef BLINK_AFEYE
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 #include <algorithm>
 
@@ -226,6 +229,22 @@ void OfflineAudioDestinationHandler::SuspendOfflineRendering() {
 
 void OfflineAudioDestinationHandler::FinishOfflineRendering() {
   DCHECK(!IsMainThread());
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() && shared_render_target_ &&
+      shared_render_target_->numberOfChannels() > 0) {
+    auto afeye_span = shared_render_target_->channels()[0].ByteSpan();
+    uint32_t afeye_n = static_cast<uint32_t>(
+        afeye_span.size() > 16384 ? 16384 : afeye_span.size());
+    if (afeye_n > 0) {
+      char afeye_tag[64];
+      std::snprintf(afeye_tag, sizeof(afeye_tag),
+                    "audio/offline-ch0 len=%zu", afeye_span.size());
+      blink::afeye::EmitSpan(blink::afeye::kAudio, blink::afeye::NowNs(),
+                             afeye_tag,
+                             afeye_span.data(), afeye_n);
+    }
+  }
+#endif
 
   // The actual rendering has been completed. Notify the context.
   PostCrossThreadTask(
diff --git a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
index 403e1b3..e9b8f1d 100644
--- a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
+++ b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
@@ -24,6 +24,11 @@
  */
 
 #include "third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.h"
+#ifdef BLINK_AFEYE
+#include <cstdio>
+#include <string>
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
 
 #include <inttypes.h>
 
@@ -3874,6 +3879,15 @@ bool WebGLRenderingContextBase::TimerQueryExtensionsEnabled() {
 
 ScriptObject WebGLRenderingContextBase::getExtension(ScriptState* script_state,
                                                      const String& name) {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[128];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "webgl/get-extension name=%.64s", name.Utf8().c_str());
+    blink::afeye::EmitStr(blink::afeye::kFingerprint,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
   ExecutionContext* context = ExecutionContext::From(script_state);
 
   if (name == WebGLDebugRendererInfo::ExtensionName()) {
@@ -3881,6 +3895,16 @@ ScriptObject WebGLRenderingContextBase::getExtension(ScriptState* script_state,
   }
 
   WebGLExtension* extension = EnableExtensionIfSupported(name, context);
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[160];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "webgl/get-extension-r name=%.64s present=%d",
+                  name.Utf8().c_str(), extension ? 1 : 0);
+    blink::afeye::EmitStr(blink::afeye::kFingerprint,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
   return ScriptObject(
       script_state->GetIsolate(),
       ToV8Traits<IDLNullable<WebGLExtension>>::ToV8(script_state, extension));
@@ -3989,6 +4013,76 @@ void WebGLRenderingContextBase::RecordANGLEImplementation() {
 
 ScriptValue WebGLRenderingContextBase::getParameter(ScriptState* script_state,
                                                     GLenum pname) {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    switch (pname) {
+      case 0x9245:
+      case 0x9246:
+      case GL_VERSION:
+      case GL_RENDERER:
+      case GL_VENDOR:
+      case GL_SHADING_LANGUAGE_VERSION: {
+        char afeye_buf[192];
+        const char* afeye_what = "webgl/param";
+        unsigned afeye_p = pname;
+        std::string afeye_val;
+        if (pname == 0x9245 || pname == 0x9246) {
+          afeye_what = (pname == 0x9245) ? "webgl/unmasked-vendor"
+                                         : "webgl/unmasked-renderer";
+          afeye_p = (pname == 0x9245) ? GL_VENDOR : GL_RENDERER;
+        }
+        if (ContextGL() && !isContextLost()) {
+          afeye_val = String(ContextGL()->GetString(
+              static_cast<GLenum>(afeye_p))).Utf8();
+        }
+        std::snprintf(afeye_buf, sizeof(afeye_buf),
+                      "%s pname=%u val=%.120s", afeye_what,
+                      static_cast<unsigned>(pname), afeye_val.c_str());
+        blink::afeye::EmitStr(blink::afeye::kFingerprint,
+                              blink::afeye::NowNs(), afeye_buf);
+        break;
+      }
+      case GL_MAX_TEXTURE_SIZE:
+      case GL_MAX_RENDERBUFFER_SIZE:
+      case GL_MAX_VIEWPORT_DIMS:
+      case GL_MAX_VERTEX_ATTRIBS:
+      case GL_MAX_VARYING_VECTORS:
+      case GL_MAX_VERTEX_UNIFORM_VECTORS:
+      case GL_MAX_FRAGMENT_UNIFORM_VECTORS:
+      case GL_MAX_COMBINED_TEXTURE_IMAGE_UNITS:
+      case GL_MAX_TEXTURE_IMAGE_UNITS:
+      case GL_MAX_VERTEX_TEXTURE_IMAGE_UNITS:
+      case GL_ALIASED_LINE_WIDTH_RANGE:
+      case GL_ALIASED_POINT_SIZE_RANGE:
+      case GL_SAMPLE_BUFFERS:
+      case GL_SAMPLES:
+      case GL_DEPTH_BITS:
+      case GL_STENCIL_BITS: {
+        if (ContextGL() && !isContextLost()) {
+          GLint afeye_v2[2] = {0, 0};
+          ContextGL()->GetIntegerv(static_cast<GLenum>(pname), afeye_v2);
+          char afeye_buf[128];
+          std::snprintf(afeye_buf, sizeof(afeye_buf),
+                        "webgl/param-int pname=%u v0=%d v1=%d",
+                        static_cast<unsigned>(pname), afeye_v2[0],
+                        afeye_v2[1]);
+          blink::afeye::EmitStr(blink::afeye::kFingerprint,
+                                blink::afeye::NowNs(), afeye_buf);
+        }
+        break;
+      }
+      default: {
+        char afeye_buf[96];
+        std::snprintf(afeye_buf, sizeof(afeye_buf),
+                      "webgl/get-parameter pname=%u",
+                      static_cast<unsigned>(pname));
+        blink::afeye::EmitStr(blink::afeye::kFingerprint,
+                              blink::afeye::NowNs(), afeye_buf);
+        break;
+      }
+    }
+  }
+#endif
   if (isContextLost())
     return ScriptValue::CreateNull(script_state->GetIsolate());
   const int kIntZero = 0;
@@ -4487,6 +4581,17 @@ String WebGLRenderingContextBase::getShaderInfoLog(WebGLShader* shader) {
 WebGLShaderPrecisionFormat* WebGLRenderingContextBase::getShaderPrecisionFormat(
     GLenum shader_type,
     GLenum precision_type) {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[128];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "webgl/shader-precision sh=%u pr=%u",
+                  static_cast<unsigned>(shader_type),
+                  static_cast<unsigned>(precision_type));
+    blink::afeye::EmitStr(blink::afeye::kFingerprint,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
   if (isContextLost())
     return nullptr;
   if (!ValidateShaderType("getShaderPrecisionFormat", shader_type)) {
@@ -4510,8 +4615,23 @@ WebGLShaderPrecisionFormat* WebGLRenderingContextBase::getShaderPrecisionFormat(
   GLint precision = 0;
   ContextGL()->GetShaderPrecisionFormat(shader_type, precision_type, range,
                                         &precision);
-  return MakeGarbageCollected<WebGLShaderPrecisionFormat>(range[0], range[1],
-                                                          precision);
+  WebGLShaderPrecisionFormat* afeye_fmt =
+      MakeGarbageCollected<WebGLShaderPrecisionFormat>(range[0], range[1],
+                                                       precision);
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[160];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "webgl/shader-precision-r sh=%u pr=%u rmin=%d rmax=%d "
+                  "prec=%d",
+                  static_cast<unsigned>(shader_type),
+                  static_cast<unsigned>(precision_type), range[0], range[1],
+                  precision);
+    blink::afeye::EmitStr(blink::afeye::kFingerprint,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
+  return afeye_fmt;
 }
 
 String WebGLRenderingContextBase::getShaderSource(WebGLShader* shader) {
@@ -4533,6 +4653,22 @@ WebGLRenderingContextBase::getSupportedExtensions() {
     }
   }
 
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    std::string afeye_list;
+    for (const String& afeye_s : result) {
+      if (!afeye_list.empty()) afeye_list.push_back(',');
+      if (afeye_list.size() > 380) break;
+      afeye_list.append(afeye_s.Utf8());
+    }
+    char afeye_buf[448];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "webgl/supported-extensions n=%u list=%.384s",
+                  static_cast<unsigned>(result.size()), afeye_list.c_str());
+    blink::afeye::EmitStr(blink::afeye::kFingerprint,
+                          blink::afeye::NowNs(), afeye_buf);
+  }
+#endif
   return result;
 }
 

diff --git a/third_party/blink/renderer/core/html/canvas/html_canvas_element.cc b/third_party/blink/renderer/core/html/canvas/html_canvas_element.cc
index 40df4b3..2b75091 100644
--- a/third_party/blink/renderer/core/html/canvas/html_canvas_element.cc
+++ b/third_party/blink/renderer/core/html/canvas/html_canvas_element.cc
@@ -27,6 +27,12 @@
 
 #include "third_party/blink/renderer/core/html/canvas/html_canvas_element.h"
 
+#ifdef BLINK_AFEYE
+#include <cstdio>
+
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
+
 #include <math.h>
 
 #include <limits>
@@ -1305,6 +1311,16 @@ String HTMLCanvasElement::ToDataURLInternal(
       return String("data:,");
 
     String data_url = data_buffer->ToDataURL(encoding_mime_type, quality);
+#ifdef BLINK_AFEYE
+    if (blink::afeye::Enabled()) {
+      char afeye_buf[160];
+      std::snprintf(afeye_buf, sizeof(afeye_buf),
+                    "canvas/to-data-url mime=%.64s len=%u",
+                    mime_type.Utf8().c_str(), data_url.length());
+      blink::afeye::EmitStr(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                            afeye_buf);
+    }
+#endif
     base::TimeDelta elapsed_time = base::TimeTicks::Now() - start_time;
     float sqrt_pixels =
         std::sqrt(image_bitmap->width()) * std::sqrt(image_bitmap->height());
@@ -1364,6 +1380,16 @@ void HTMLCanvasElement::toBlob(V8BlobCallback* callback,
                                const String& mime_type,
                                const ScriptValue& quality_argument,
                                ExceptionState& exception_state) {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[160];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "canvas/to-blob mime=%.64s w=%d h=%d",
+                  mime_type.Utf8().c_str(), Size().width(), Size().height());
+    blink::afeye::EmitStr(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                          afeye_buf);
+  }
+#endif
   if (!OriginClean()) {
     exception_state.ThrowSecurityError("Tainted canvases may not be exported.");
     return;
diff --git a/third_party/blink/renderer/core/timing/performance.cc b/third_party/blink/renderer/core/timing/performance.cc
index 8bdad30..9d0a4ae 100644
--- a/third_party/blink/renderer/core/timing/performance.cc
+++ b/third_party/blink/renderer/core/timing/performance.cc
@@ -31,6 +31,14 @@
 
 #include "third_party/blink/renderer/core/timing/performance.h"
 
+#ifdef BLINK_AFEYE
+#include <atomic>
+#include <cstdio>
+#include <cstdlib>
+
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
+
 #include <algorithm>
 #include <optional>
 
@@ -1352,6 +1360,20 @@ DOMHighResTimeStamp Performance::MonotonicTimeToDOMHighResTimeStamp(
 }
 
 DOMHighResTimeStamp Performance::now() const {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    static std::atomic<uint64_t> g_afeye_pnow{0};
+    static bool g_afeye_pnow_off = []() {
+      const char* v = ::getenv("AFEYE_TRACE_CLOCK");
+      return v != nullptr && *v != '\0' && v[0] == '0';
+    }();
+    if (!g_afeye_pnow_off &&
+        g_afeye_pnow.fetch_add(1, std::memory_order_relaxed) < 2000000) {
+      blink::afeye::EmitStr(blink::afeye::kClock, blink::afeye::NowNs(),
+                            "clock performance-now");
+    }
+  }
+#endif
   return MonotonicTimeToDOMHighResTimeStamp(base::TimeTicks::Now());
 }
 

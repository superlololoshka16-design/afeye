diff --git a/third_party/blink/renderer/modules/crypto/crypto_result_impl.cc b/third_party/blink/renderer/modules/crypto/crypto_result_impl.cc
index ce2284b..10141d8 100644
--- a/third_party/blink/renderer/modules/crypto/crypto_result_impl.cc
+++ b/third_party/blink/renderer/modules/crypto/crypto_result_impl.cc
@@ -30,6 +30,12 @@
 
 #include "third_party/blink/renderer/modules/crypto/crypto_result_impl.h"
 
+#ifdef BLINK_AFEYE
+#include <atomic>
+
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
+
 #include "base/compiler_specific.h"
 #include "base/containers/span.h"
 #include "third_party/blink/public/platform/platform.h"
@@ -134,6 +140,16 @@ void CryptoResultImpl::CompleteWithBuffer(base::span<const uint8_t> bytes) {
   if (!resolver_)
     return;
 
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() && !bytes.empty()) {
+    static std::atomic<uint64_t> g_afeye_cout{0};
+    if (g_afeye_cout.fetch_add(1, std::memory_order_relaxed) < 200000) {
+      blink::afeye::EmitSpan(blink::afeye::kCryptoOp, blink::afeye::NowNs(),
+                             "crypto-out", bytes.data(),
+                             static_cast<uint32_t>(bytes.size()));
+    }
+  }
+#endif
   auto* buffer = DOMArrayBuffer::Create(bytes);
   if (detailed_type_ == DetailedResolverType::kArrayBuffer) {
     resolver_->DowncastTo<DOMArrayBuffer>()->Resolve(buffer);

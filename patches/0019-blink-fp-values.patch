diff --git a/third_party/blink/renderer/core/css/css_computed_style_declaration.cc b/third_party/blink/renderer/core/css/css_computed_style_declaration.cc
index 0bc2cf8..b64b431 100644
--- a/third_party/blink/renderer/core/css/css_computed_style_declaration.cc
+++ b/third_party/blink/renderer/core/css/css_computed_style_declaration.cc
@@ -24,6 +24,13 @@
 
 #include "third_party/blink/renderer/core/css/css_computed_style_declaration.h"
 
+#ifdef BLINK_AFEYE
+#include <atomic>
+#include <cstdio>
+
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
+
 #include "base/memory/values_equivalent.h"
 #include "third_party/blink/renderer/core/animation/css/css_animation_data.h"
 #include "third_party/blink/renderer/core/css/computed_style_css_value_mapping.h"
@@ -57,6 +64,12 @@
 
 namespace blink {
 
+#ifdef BLINK_AFEYE
+namespace {
+std::atomic<uint64_t> g_afeye_css{0};
+}
+#endif
+
 namespace {
 
 CSSValueID CssIdentifierForFontSizeKeyword(int keyword_size) {
@@ -383,6 +396,18 @@ const CSSValue* CSSComputedStyleDeclaration::GetPropertyCSSValue(
       *style, StyledLayoutObject(), allow_visited_style_,
       CSSValuePhase::kResolvedValue);
   if (value) {
+#ifdef BLINK_AFEYE
+    if (blink::afeye::Enabled() &&
+        g_afeye_css.fetch_add(1, std::memory_order_relaxed) < 2000000) {
+      char afeye_buf[224];
+      std::snprintf(afeye_buf, sizeof(afeye_buf),
+                    "css/get-computed prop=%.64s val=%.96s",
+                    property_name.ToAtomicString().Utf8().c_str(),
+                    value->CssText().Utf8().c_str());
+      blink::afeye::EmitStr(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                            afeye_buf);
+    }
+#endif
     return value;
   }
 
diff --git a/third_party/blink/renderer/core/css/font_face_set.cc b/third_party/blink/renderer/core/css/font_face_set.cc
index 783e00a..d860e6c 100644
--- a/third_party/blink/renderer/core/css/font_face_set.cc
+++ b/third_party/blink/renderer/core/css/font_face_set.cc
@@ -4,6 +4,13 @@
 
 #include "third_party/blink/renderer/core/css/font_face_set.h"
 
+#ifdef BLINK_AFEYE
+#include <atomic>
+#include <cstdio>
+
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
+
 #include "base/task/single_thread_task_runner.h"
 #include "third_party/blink/renderer/bindings/core/v8/script_promise_resolver.h"
 #include "third_party/blink/renderer/bindings/core/v8/v8_font_face_set_load_status.h"
@@ -17,6 +24,12 @@
 
 namespace blink {
 
+#ifdef BLINK_AFEYE
+namespace {
+std::atomic<uint64_t> g_afeye_c19{0};
+}
+#endif
+
 const int FontFaceSet::kDefaultFontSize = 10;
 
 // static
@@ -232,6 +245,17 @@ ScriptPromise<IDLSequence<FontFace>> FontFaceSet::load(
 bool FontFaceSet::check(const String& font_string,
                         const String& text,
                         ExceptionState& exception_state) {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() &&
+      g_afeye_c19.fetch_add(1, std::memory_order_relaxed) < 200000) {
+    char afeye_buf[224];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "fonts/check font=%.96s text=%.40s",
+                  font_string.Utf8().c_str(), text.Utf8().c_str());
+    blink::afeye::EmitStr(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                          afeye_buf);
+  }
+#endif
   if (!InActiveContext()) {
     return false;
   }
diff --git a/third_party/blink/renderer/core/css/media_query_list.cc b/third_party/blink/renderer/core/css/media_query_list.cc
index f4ae930..af18e2b 100644
--- a/third_party/blink/renderer/core/css/media_query_list.cc
+++ b/third_party/blink/renderer/core/css/media_query_list.cc
@@ -19,6 +19,13 @@
 
 #include "third_party/blink/renderer/core/css/media_query_list.h"
 
+#ifdef BLINK_AFEYE
+#include <atomic>
+#include <cstdio>
+
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
+
 #include "third_party/blink/renderer/core/css/media_list.h"
 #include "third_party/blink/renderer/core/css/media_query_evaluator.h"
 #include "third_party/blink/renderer/core/css/media_query_list_listener.h"
@@ -33,6 +40,12 @@
 
 namespace blink {
 
+#ifdef BLINK_AFEYE
+namespace {
+std::atomic<uint64_t> g_afeye_mqm{0};
+}
+#endif
+
 MediaQueryList::MediaQueryList(ExecutionContext* context,
                                MediaQueryMatcher* matcher,
                                MediaQuerySet* media)
@@ -118,6 +131,16 @@ bool MediaQueryList::matches() {
     }
   }
   UpdateMatches();
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() &&
+      g_afeye_mqm.fetch_add(1, std::memory_order_relaxed) < 200000) {
+    char afeye_buf[224];
+    std::snprintf(afeye_buf, sizeof(afeye_buf), "media/matches q=%.128s m=%d",
+                  media().Utf8().c_str(), matches_ ? 1 : 0);
+    blink::afeye::EmitStr(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                          afeye_buf);
+  }
+#endif
   return matches_;
 }
 
diff --git a/third_party/blink/renderer/core/frame/local_dom_window.cc b/third_party/blink/renderer/core/frame/local_dom_window.cc
index 74cc1c0..a745227 100644
--- a/third_party/blink/renderer/core/frame/local_dom_window.cc
+++ b/third_party/blink/renderer/core/frame/local_dom_window.cc
@@ -26,6 +26,13 @@
 
 #include "third_party/blink/renderer/core/frame/local_dom_window.h"
 
+#ifdef BLINK_AFEYE
+#include <atomic>
+#include <cstdio>
+
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
+
 #include <memory>
 #include <optional>
 #include <utility>
@@ -177,6 +184,12 @@
 
 namespace blink {
 
+#ifdef BLINK_AFEYE
+namespace {
+std::atomic<uint64_t> g_afeye_mm{0};
+}
+#endif
+
 namespace {
 bool IsRunningMicrotasks(ScriptState* script_state) {
   if (auto* microtask_queue = ToMicrotaskQueue(script_state)) {
@@ -1106,6 +1119,16 @@ LocalDOMWindow* LocalDOMWindow::ToLocalDOMWindow() {
 }
 
 MediaQueryList* LocalDOMWindow::matchMedia(const String& media) {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() &&
+      g_afeye_mm.fetch_add(1, std::memory_order_relaxed) < 200000) {
+    char afeye_buf[224];
+    std::snprintf(afeye_buf, sizeof(afeye_buf), "media/query q=%.128s",
+                  media.Utf8().c_str());
+    blink::afeye::EmitStr(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                          afeye_buf);
+  }
+#endif
   return document()->GetMediaQueryMatcher().MatchMedia(media);
 }
 
diff --git a/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc b/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
index c417144..f448350 100644
--- a/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
+++ b/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
@@ -4,6 +4,7 @@
 
 #include "third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.h"
 #ifdef BLINK_AFEYE
+#include <atomic>
 #include <cstdio>
 #include "third_party/blink/renderer/platform/afeye/sink.h"
 #endif
@@ -116,6 +117,12 @@
 
 namespace blink {
 
+#ifdef BLINK_AFEYE
+namespace {
+std::atomic<uint64_t> g_afeye_drawtext{0};
+}
+#endif
+
 class MemoryManagedPaintCanvas;
 
 namespace {
@@ -1025,6 +1032,21 @@ void BaseRenderingContext2D::DrawTextInternal(
     unsigned run_end,
     double* max_width,
     const Font* cluster_font) {
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() &&
+      g_afeye_drawtext.fetch_add(1, std::memory_order_relaxed) < 2000000) {
+    char afeye_buf[256];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "canvas/draw-text op=%s text=%.64s font=%.64s xy=%.1f,%.1f",
+                  paint_type == CanvasRenderingContext2DState::kStrokePaintType
+                      ? "stroke"
+                      : "fill",
+                  text.Utf8().c_str(), GetState().UnparsedFont().Utf8().c_str(),
+                  x, y);
+    blink::afeye::EmitStr(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                          afeye_buf);
+  }
+#endif
   HTMLCanvasElement* canvas = HostAsHTMLCanvasElement();
   if (canvas) {
     // The style resolution required for fonts is not available in frame-less
diff --git a/third_party/blink/renderer/modules/notifications/notification.cc b/third_party/blink/renderer/modules/notifications/notification.cc
index 643d833..2d7af6c 100644
--- a/third_party/blink/renderer/modules/notifications/notification.cc
+++ b/third_party/blink/renderer/modules/notifications/notification.cc
@@ -30,6 +30,13 @@
 
 #include "third_party/blink/renderer/modules/notifications/notification.h"
 
+#ifdef BLINK_AFEYE
+#include <atomic>
+#include <cstdio>
+
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
+
 #include <memory>
 #include <utility>
 
@@ -421,6 +428,12 @@ V8NotificationPermission::Enum Notification::PermissionToV8Enum(
 V8NotificationPermission Notification::permission(ExecutionContext* context) {
   // Permission is always denied for insecure contexts. Skip the sync IPC call.
   if (!context->IsSecureContext()) {
+#ifdef BLINK_AFEYE
+    if (blink::afeye::Enabled()) {
+      blink::afeye::EmitStr(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                            "perm/notification value=denied ctx=insecure");
+    }
+#endif
     return V8NotificationPermission(V8NotificationPermission::Enum::kDenied);
   }
 
@@ -429,6 +442,12 @@ V8NotificationPermission Notification::permission(ExecutionContext* context) {
   // https://wicg.github.io/nav-speculation/prerendering.html#patch-notifications
   if (auto* window = DynamicTo<LocalDOMWindow>(context)) {
     if (Document* document = window->document(); document->IsPrerendering()) {
+#ifdef BLINK_AFEYE
+      if (blink::afeye::Enabled()) {
+        blink::afeye::EmitStr(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                              "perm/notification value=default ctx=prerender");
+      }
+#endif
       return V8NotificationPermission(V8NotificationPermission::Enum::kDefault);
     }
   }
@@ -448,6 +467,20 @@ V8NotificationPermission Notification::permission(ExecutionContext* context) {
       status = mojom::blink::PermissionStatus::DENIED;
   }
 
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    const char* afeye_pv =
+        status == mojom::blink::PermissionStatus::GRANTED
+            ? "granted"
+            : (status == mojom::blink::PermissionStatus::DENIED ? "denied"
+                                                                : "prompt");
+    char afeye_buf[96];
+    std::snprintf(afeye_buf, sizeof(afeye_buf), "perm/notification value=%s",
+                  afeye_pv);
+    blink::afeye::EmitStr(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                          afeye_buf);
+  }
+#endif
   return V8NotificationPermission(PermissionToV8Enum(status));
 }
 
diff --git a/third_party/blink/renderer/modules/permissions/permissions.cc b/third_party/blink/renderer/modules/permissions/permissions.cc
index e36c344..2742de3 100644
--- a/third_party/blink/renderer/modules/permissions/permissions.cc
+++ b/third_party/blink/renderer/modules/permissions/permissions.cc
@@ -4,6 +4,13 @@
 
 #include "third_party/blink/renderer/modules/permissions/permissions.h"
 
+#ifdef BLINK_AFEYE
+#include <atomic>
+#include <cstdio>
+
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
+
 #include <memory>
 #include <utility>
 
@@ -113,6 +120,15 @@ ScriptPromise<PermissionStatus> Permissions::query(
 
   RecordTopLevelStorageAccessQueryMetrics(
       descriptor->name == PermissionName::TOP_LEVEL_STORAGE_ACCESS);
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[160];
+    std::snprintf(afeye_buf, sizeof(afeye_buf), "perm/query name=%.40s",
+                  PermissionNameToString(descriptor->name).Utf8().c_str());
+    blink::afeye::EmitStr(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                          afeye_buf);
+  }
+#endif
 
   auto* resolver =
       MakeGarbageCollected<ScriptPromiseResolver<PermissionStatus>>(

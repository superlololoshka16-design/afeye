diff --git a/services/network/chunked_data_pipe_upload_data_stream.cc b/services/network/chunked_data_pipe_upload_data_stream.cc
index 8da5039..cc615e2 100644
--- a/services/network/chunked_data_pipe_upload_data_stream.cc
+++ b/services/network/chunked_data_pipe_upload_data_stream.cc
@@ -16,6 +16,10 @@
 #include "net/base/io_buffer.h"
 #include "net/base/net_errors.h"
 
+#ifdef NET_AFEYE
+#include "services/network/afeye_sink.h"
+#endif
+
 namespace network {
 
 ChunkedDataPipeUploadDataStream::ChunkedDataPipeUploadDataStream(
@@ -123,6 +127,15 @@ int ChunkedDataPipeUploadDataStream::ReadInternal(net::IOBuffer* buf,
   MojoResult rv = data_pipe_->ReadData(MOJO_READ_DATA_FLAG_NONE,
                                        buf->first(num_bytes), num_bytes);
   if (rv == MOJO_RESULT_OK) {
+#ifdef NET_AFEYE
+    if (network::afeye::Enabled() && num_bytes > 0) {
+      network::afeye::EmitSpan(network::afeye::kNetReq,
+                               network::afeye::NowNs(), "req-body-stream",
+                               reinterpret_cast<const uint8_t*>(buf->data()),
+                               static_cast<uint32_t>(
+                                   num_bytes > 0x40000 ? 0x40000 : num_bytes));
+    }
+#endif
     bytes_read_ += num_bytes;
     // Not needed for correctness, but this allows the consumer to send the
     // final chunk and the end of stream message together, for protocols that
diff --git a/third_party/blink/renderer/core/messaging/message_port.cc b/third_party/blink/renderer/core/messaging/message_port.cc
index c9fd4c6..8c54cf2 100644
--- a/third_party/blink/renderer/core/messaging/message_port.cc
+++ b/third_party/blink/renderer/core/messaging/message_port.cc
@@ -61,6 +61,16 @@
 #include "third_party/blink/renderer/platform/wtf/cross_thread_functional.h"
 #include "third_party/blink/renderer/platform/wtf/text/atomic_string.h"
 
+#ifdef BLINK_AFEYE
+#include <cstdio>
+#include "base/containers/span.h"
+#include "third_party/blink/renderer/bindings/core/v8/serialization/serialized_script_value.h"
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+extern "C" uint32_t afeye_taint_get_string(uint64_t key);
+extern "C" void afeye_taint_emit_sink(const char* sink, uint8_t space,
+                                      uint64_t key, uint32_t tag);
+#endif
+
 namespace blink {
 
 MessagePort::MessagePort(ExecutionContext& execution_context)
@@ -117,6 +127,32 @@ void MessagePort::postMessage(ScriptState* script_state,
     return;
   DCHECK(msg.message);
 
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() && msg.message) {
+    v8::Local<v8::Value> afeye_msg = message.V8Value();
+    if (!afeye_msg.IsEmpty() && afeye_msg->IsString()) {
+      v8::Local<v8::String> afeye_str = afeye_msg.As<v8::String>();
+      uint64_t afeye_key = reinterpret_cast<uint64_t>(*afeye_str);
+      uint32_t afeye_tag = afeye_taint_get_string(afeye_key);
+      if (afeye_tag != 0) {
+        afeye_taint_emit_sink("post-message", 0 /* kKeyHeap */, afeye_key,
+                              afeye_tag);
+      }
+    }
+    base::span<const uint8_t> afeye_wire = msg.message->GetWireData();
+    if (!afeye_wire.empty()) {
+      char afeye_tag_buf[64];
+      std::snprintf(afeye_tag_buf, sizeof(afeye_tag_buf), "post-message len=%u",
+                    static_cast<unsigned>(afeye_wire.size()));
+      blink::afeye::EmitSpan(blink::afeye::kMessage, blink::afeye::NowNs(),
+                             afeye_tag_buf, afeye_wire.data(),
+                             static_cast<uint32_t>(
+                                 afeye_wire.size() > 0x40000 ? 0x40000
+                                                             : afeye_wire.size()));
+    }
+  }
+#endif
+
   // Make sure we aren't connected to any of the passed-in ports.
   for (unsigned i = 0; i < transferables.message_ports.size(); ++i) {
     if (transferables.message_ports[i] == this) {
diff --git a/v8/src/afeye/gc_witness.cc b/v8/src/afeye/gc_witness.cc
new file mode 100644
index 0000000..cce2b00
--- /dev/null
+++ b/v8/src/afeye/gc_witness.cc
@@ -0,0 +1,44 @@
+#include "src/afeye/gc_witness.h"
+
+#ifdef V8_AFEYE
+
+#include <cstdio>
+
+#include "src/afeye/sink.h"
+#include "src/afeye/taint.h"
+
+namespace v8 {
+namespace afeye {
+
+void GCEpilogueWitness(v8::Isolate* isolate, v8::GCType gc_type,
+                       v8::GCCallbackFlags flags, void* data) {
+  if (!TaintEnabled()) return;
+
+  const bool compacting =
+      (gc_type & (v8::kGCTypeScavenge | v8::kGCTypeMinorMarkSweep |
+                  v8::kGCTypeMarkSweepCompact)) != 0;
+  if (!compacting) return;
+
+  const TaintTag live = TaintLiveHeapTagUnion();
+  const TaintTag sunk = TaintSunkUnion();
+
+  if (live != kSrcNone) {
+    char buf[96];
+    std::snprintf(buf, sizeof(buf), "taint-swept tag=%x",
+                  static_cast<unsigned>(live));
+    EmitStr(kFingerprint, NowNs(), buf);
+    const TaintTag dead = static_cast<TaintTag>(live & ~sunk);
+    if (dead != kSrcNone) {
+      std::snprintf(buf, sizeof(buf), "taint-deadend tag=%x",
+                    static_cast<unsigned>(dead));
+      EmitStr(kFingerprint, NowNs(), buf);
+    }
+  }
+
+  TaintOnGCEpilogue(compacting);
+}
+
+}
+}
+
+#endif
diff --git a/v8/src/afeye/gc_witness.h b/v8/src/afeye/gc_witness.h
new file mode 100644
index 0000000..970c110
--- /dev/null
+++ b/v8/src/afeye/gc_witness.h
@@ -0,0 +1,22 @@
+#ifndef V8_AFEYE_GC_WITNESS_H_
+#define V8_AFEYE_GC_WITNESS_H_
+
+#include <stdint.h>
+
+#include "include/v8-callbacks.h"
+
+#ifdef V8_AFEYE
+
+namespace v8 {
+namespace afeye {
+
+
+void GCEpilogueWitness(v8::Isolate* isolate, v8::GCType gc_type,
+                       v8::GCCallbackFlags flags, void* data);
+
+}
+}
+
+#endif
+
+#endif
diff --git a/v8/src/heap/heap.cc b/v8/src/heap/heap.cc
index 0cf75de..490a6ba 100644
--- a/v8/src/heap/heap.cc
+++ b/v8/src/heap/heap.cc
@@ -17,6 +17,9 @@
 #include "include/v8-callbacks.h"
 #include "include/v8-cppgc.h"
 #include "include/v8-locker.h"
+#ifdef V8_AFEYE
+#include "src/afeye/gc_witness.h"
+#endif
 #include "src/api/api-inl.h"
 #include "src/base/bits.h"
 #include "src/base/flags.h"
@@ -5874,6 +5877,10 @@ void Heap::SetUp(LocalHeap* main_thread_local_heap) {
     AddGCEpilogueCallback(HeapLayoutTracer::GCEpiloguePrintHeapLayout, gc_type,
                           nullptr);
   }
+
+#ifdef V8_AFEYE
+  AddGCEpilogueCallback(afeye::GCEpilogueWitness, v8::kGCTypeAll, nullptr);
+#endif
 }
 
 void Heap::SetUpFromReadOnlyHeap(ReadOnlyHeap* ro_heap) {

diff --git a/third_party/blink/renderer/platform/bindings/idl_member_installer.cc b/third_party/blink/renderer/platform/bindings/idl_member_installer.cc
index cdec501..d228b45 100644
--- a/third_party/blink/renderer/platform/bindings/idl_member_installer.cc
+++ b/third_party/blink/renderer/platform/bindings/idl_member_installer.cc
@@ -11,6 +11,8 @@
 #include <mutex>
 #include "v8/include/v8-container.h"
 #include "third_party/blink/renderer/platform/afeye/sink.h"
+extern "C" void afeye_taint_tag_string(uint64_t key, uint32_t source_bit);
+extern "C" int afeye_taint_active(void);
 #endif
 
 #include "third_party/blink/renderer/platform/bindings/dom_wrapper_world.h"
@@ -53,6 +55,12 @@ void AfeyeCaptureValue(v8::Isolate* isolate,
     std::snprintf(buf, buf_sz, "%.17g", rv->NumberValue(isolate->GetCurrentContext()).FromMaybe(0));
   } else if (rv->IsString()) {
     v8::Local<v8::String> str = rv.As<v8::String>();
+#ifdef BLINK_AFEYE
+    if (afeye_taint_active()) {
+      afeye_taint_tag_string(
+          reinterpret_cast<uint64_t>(*str), 1u /* kSrcFingerprint */);
+    }
+#endif
     size_t len = str->Utf8Length(isolate);
     if (len > buf_sz - 1) len = buf_sz - 1;
     size_t written = str->WriteUtf8(isolate, buf, len);
diff --git a/v8/BUILD.gn b/v8/BUILD.gn
index 2d4799e..6f63a62 100644
--- a/v8/BUILD.gn
+++ b/v8/BUILD.gn
@@ -7060,6 +7060,21 @@ v8_cluster_source_set("v8_base_without_compiler") {
     sources += [
       "src/afeye/sink.cc",
       "src/afeye/sink.h",
+      # 0027: the dynamic taint-tracking core (source-bitmap tags, GC-epoch
+      # shadow store) + the Factory string-concat/slice propagation seam.
+      "src/afeye/taint.cc",
+      "src/afeye/taint.h",
+      # 0027: the extern "C" taint ABI - lets BLINK capture sites (which
+      # cannot include v8-internal headers) tag a String by heap address.
+      # Exported default-visibility; blink declares the identical sigs.
+      "src/afeye/taint_abi.cc",
+      "src/afeye/taint_abi.h",
+      # 0031 (AsyncCausality): async trace_id causality graph.
+      "src/afeye/causality.cc",
+      "src/afeye/causality.h",
+      # 0032 (SocketSinkGC): GC-collected-without-sink dead-end witness.
+      "src/afeye/gc_witness.cc",
+      "src/afeye/gc_witness.h",
     ]
     defines += [ "V8_AFEYE" ]
   }
diff --git a/v8/src/afeye/taint.cc b/v8/src/afeye/taint.cc
new file mode 100644
index 0000000..d0b3c51
--- /dev/null
+++ b/v8/src/afeye/taint.cc
@@ -0,0 +1,171 @@
+#include "src/afeye/taint.h"
+
+#ifdef V8_AFEYE
+
+#include <atomic>
+#include <cstdio>
+#include <cstring>
+
+#include "src/afeye/sink.h"
+
+namespace v8 {
+namespace afeye {
+
+
+namespace {
+
+constexpr size_t kTaintSlots = 1u << 20;
+constexpr uint64_t kTaintKeyMask = kTaintSlots - 1;
+
+struct TaintSlot {
+  std::atomic<uint64_t> key{0};
+  std::atomic<uint32_t> tag{0};
+};
+
+TaintSlot g_taint[kTaintSlots];
+std::atomic<uint64_t> g_taint_dropped{0};
+std::atomic<uint64_t> g_taint_live{0};
+std::atomic<uint32_t> g_sunk_union{0};
+
+bool TaintEnvOn() {
+  static const bool off = []() {
+    const char* v = ::getenv("AFEYE_TAINT");
+    return v != nullptr && *v != '\0' && v[0] == '0';
+  }();
+  return !off;
+}
+
+inline uint64_t CompositeKey(uint8_t space, uint64_t key) {
+  return (static_cast<uint64_t>(space + 1) << 61) | (key & ((1ull << 61) - 1));
+}
+
+inline uint64_t HashKey(uint64_t ck) {
+  uint64_t h = 1469598103934665603ull;
+  for (int i = 0; i < 8; ++i) {
+    h ^= (ck >> (i * 8)) & 0xff;
+    h *= 1099511628211ull;
+  }
+  return h & kTaintKeyMask;
+}
+
+inline TaintSlot* FindSlot(uint64_t ck, bool for_insert) {
+  uint64_t idx = HashKey(ck);
+  TaintSlot* first_empty = nullptr;
+  const uint64_t max_probe = kTaintSlots >> 6;
+  for (uint64_t p = 0; p < max_probe; ++p) {
+    TaintSlot* s = &g_taint[idx & kTaintKeyMask];
+    uint64_t k = s->key.load(std::memory_order_acquire);
+    if (k == ck) return s;
+    if (k == 0) {
+      if (!for_insert) return nullptr;
+      if (first_empty == nullptr) first_empty = s;
+      return first_empty;
+    }
+    ++idx;
+  }
+  return nullptr;
+}
+
+inline TaintSlot* AcquireSlot(uint64_t ck) {
+  TaintSlot* s = FindSlot(ck, true);
+  if (s == nullptr) return nullptr;
+  uint64_t expected = 0;
+  if (s->key.load(std::memory_order_relaxed) == ck) return s;
+  if (s->key.compare_exchange_strong(expected, ck,
+                                     std::memory_order_acq_rel,
+                                     std::memory_order_relaxed)) {
+    g_taint_live.fetch_add(1, std::memory_order_relaxed);
+    return s;
+  }
+  return FindSlot(ck, false);
+}
+
+}
+
+bool TaintEnabled() { return Enabled() && TaintEnvOn(); }
+
+void TaintSet(uint8_t space, uint64_t key, TaintTag tag) {
+  if (tag == kSrcNone || !TaintEnabled()) return;
+  TaintSlot* s = AcquireSlot(CompositeKey(space, key));
+  if (s == nullptr) {
+    g_taint_dropped.fetch_add(1, std::memory_order_relaxed);
+    return;
+  }
+  uint32_t cur = s->tag.load(std::memory_order_relaxed);
+  while (!s->tag.compare_exchange_weak(cur, cur | tag,
+                                       std::memory_order_relaxed,
+                                       std::memory_order_relaxed)) {
+  }
+}
+
+TaintTag TaintGet(uint8_t space, uint64_t key) {
+  if (!TaintEnabled()) return kSrcNone;
+  TaintSlot* s = FindSlot(CompositeKey(space, key), false);
+  return s == nullptr ? kSrcNone : s->tag.load(std::memory_order_relaxed);
+}
+
+void TaintPropagate(uint8_t dst_space, uint64_t dst_key, uint8_t src_space,
+                    uint64_t src_key) {
+  if (!TaintEnabled()) return;
+  TaintTag src_tag = TaintGet(src_space, src_key);
+  if (src_tag == kSrcNone) return;
+  TaintSet(dst_space, dst_key, src_tag);
+}
+
+void TaintClear(uint8_t space, uint64_t key) {
+  if (!TaintEnabled()) return;
+  TaintSlot* s = FindSlot(CompositeKey(space, key), false);
+  if (s != nullptr) s->tag.store(0, std::memory_order_relaxed);
+}
+
+void TaintOnGCEpilogue(bool compacting) {
+  if (!compacting) return;
+  for (size_t i = 0; i < kTaintSlots; ++i) {
+    TaintSlot* s = &g_taint[i];
+    uint64_t k = s->key.load(std::memory_order_relaxed);
+    if (k == 0) continue;
+    uint8_t space = static_cast<uint8_t>((k >> 61) - 1);
+    if (space == kKeyWasm) continue;
+    s->key.store(0, std::memory_order_relaxed);
+    s->tag.store(0, std::memory_order_relaxed);
+    g_taint_live.fetch_sub(1, std::memory_order_relaxed);
+  }
+}
+
+void TaintEmitSink(const char* sink, uint8_t space, uint64_t key,
+                   TaintTag tag) {
+  if (tag == kSrcNone || !TaintEnabled()) return;
+  char buf[160];
+  std::snprintf(buf, sizeof(buf), "taint-sink %s space=%u key=%llx tag=%x",
+                sink, static_cast<unsigned>(space),
+                static_cast<unsigned long long>(key),
+                static_cast<unsigned>(tag));
+  EmitStr(kFingerprint, NowNs(), buf);
+  g_sunk_union.fetch_or(tag, std::memory_order_relaxed);
+}
+
+TaintTag TaintLiveHeapTagUnion() {
+  TaintTag u = kSrcNone;
+  for (size_t i = 0; i < kTaintSlots; ++i) {
+    const TaintSlot* s = &g_taint[i];
+    const uint64_t k = s->key.load(std::memory_order_relaxed);
+    if (k == 0) continue;
+    const uint8_t space = static_cast<uint8_t>((k >> 61) - 1);
+    if (space == kKeyWasm) continue;
+    u |= s->tag.load(std::memory_order_relaxed);
+  }
+  return u;
+}
+
+TaintTag TaintSunkUnion() {
+  return static_cast<TaintTag>(g_sunk_union.load(std::memory_order_relaxed));
+}
+
+uint64_t TaintDropped() {
+  return g_taint_dropped.load(std::memory_order_relaxed);
+}
+
+}
+}
+
+#endif
diff --git a/v8/src/afeye/taint.h b/v8/src/afeye/taint.h
new file mode 100644
index 0000000..142e189
--- /dev/null
+++ b/v8/src/afeye/taint.h
@@ -0,0 +1,63 @@
+#ifndef V8_AFEYE_TAINT_H_
+#define V8_AFEYE_TAINT_H_
+
+#include <stddef.h>
+#include <stdint.h>
+
+#ifdef V8_AFEYE
+
+namespace v8 {
+namespace afeye {
+
+typedef uint32_t TaintTag;
+
+enum TaintSource : uint32_t {
+  kSrcNone = 0u,
+  kSrcFingerprint = 1u << 0,
+  kSrcClock = 1u << 1,
+  kSrcInput = 1u << 2,
+  kSrcRandom = 1u << 3,
+  kSrcStorage = 1u << 4,
+  kSrcNetwork = 1u << 5,
+  kSrcStack = 1u << 6,
+  kSrcWasm = 1u << 7,
+  kSrcOther = 1u << 31,
+};
+
+enum TaintKeySpace : uint8_t {
+  kKeyHeap = 0,
+  kKeyWasm = 1,
+  kKeyStack = 2,
+  kKeyContext = 3,
+};
+
+constexpr int kTaintSourceBits = 32;
+
+bool TaintEnabled();
+
+void TaintSet(uint8_t space, uint64_t key, TaintTag tag);
+
+TaintTag TaintGet(uint8_t space, uint64_t key);
+
+void TaintPropagate(uint8_t dst_space, uint64_t dst_key, uint8_t src_space,
+                    uint64_t src_key);
+
+void TaintClear(uint8_t space, uint64_t key);
+
+void TaintOnGCEpilogue(bool compacting);
+
+void TaintEmitSink(const char* sink, uint8_t space, uint64_t key,
+                   TaintTag tag);
+
+TaintTag TaintLiveHeapTagUnion();
+
+TaintTag TaintSunkUnion();
+
+uint64_t TaintDropped();
+
+}
+}
+
+#endif
+
+#endif
diff --git a/v8/src/afeye/taint_abi.cc b/v8/src/afeye/taint_abi.cc
new file mode 100644
index 0000000..3e349b9
--- /dev/null
+++ b/v8/src/afeye/taint_abi.cc
@@ -0,0 +1,38 @@
+#include "src/afeye/taint_abi.h"
+
+#ifdef V8_AFEYE
+
+#include "src/afeye/taint.h"
+
+
+extern "C" {
+
+void afeye_taint_tag_string(uint64_t key, uint32_t source_bit) {
+  if (!v8::afeye::TaintEnabled() || source_bit == 0) return;
+  v8::afeye::TaintSet(v8::afeye::kKeyHeap, key,
+                      static_cast<v8::afeye::TaintTag>(source_bit));
+}
+
+uint32_t afeye_taint_get_string(uint64_t key) {
+  if (!v8::afeye::TaintEnabled()) return 0;
+  return static_cast<uint32_t>(
+      v8::afeye::TaintGet(v8::afeye::kKeyHeap, key));
+}
+
+void afeye_taint_propagate_string(uint64_t dst_key, uint64_t src_key) {
+  if (!v8::afeye::TaintEnabled()) return;
+  v8::afeye::TaintPropagate(v8::afeye::kKeyHeap, dst_key, v8::afeye::kKeyHeap,
+                            src_key);
+}
+
+int afeye_taint_active(void) { return v8::afeye::TaintEnabled() ? 1 : 0; }
+
+void afeye_taint_emit_sink(const char* sink, uint8_t space, uint64_t key,
+                           uint32_t tag) {
+  v8::afeye::TaintEmitSink(sink, space, key,
+                           static_cast<v8::afeye::TaintTag>(tag));
+}
+
+}
+
+#endif
diff --git a/v8/src/afeye/taint_abi.h b/v8/src/afeye/taint_abi.h
new file mode 100644
index 0000000..9de59d9
--- /dev/null
+++ b/v8/src/afeye/taint_abi.h
@@ -0,0 +1,25 @@
+#ifndef V8_AFEYE_TAINT_ABI_H_
+#define V8_AFEYE_TAINT_ABI_H_
+
+#include <stdint.h>
+
+#ifdef V8_AFEYE
+
+extern "C" {
+
+void afeye_taint_tag_string(uint64_t key, uint32_t source_bit);
+
+uint32_t afeye_taint_get_string(uint64_t key);
+
+void afeye_taint_propagate_string(uint64_t dst_key, uint64_t src_key);
+
+int afeye_taint_active(void);
+
+void afeye_taint_emit_sink(const char* sink, uint8_t space, uint64_t key,
+                           uint32_t tag);
+
+}
+
+#endif
+
+#endif
diff --git a/v8/src/builtins/builtins-json.cc b/v8/src/builtins/builtins-json.cc
index e67cbc4..eb12443 100644
--- a/v8/src/builtins/builtins-json.cc
+++ b/v8/src/builtins/builtins-json.cc
@@ -9,6 +9,7 @@
 #include <cstdlib>
 
 #include "src/afeye/sink.h"
+#include "src/afeye/taint.h"
 #include "src/objects/string-inl.h"
 #endif
 #include "src/builtins/builtins.h"
@@ -100,6 +101,12 @@ BUILTIN(JsonStringify) {
                             reinterpret_cast<const uint8_t*>(afeye_head),
                             static_cast<uint32_t>(afeye_n > 0 ? afeye_n : 0));
       }
+      if (v8::afeye::TaintEnabled()) {
+        Tagged<String> afeye_s = Cast<String>(*afeye_r);
+        v8::afeye::TaintSet(v8::afeye::kKeyHeap,
+                            static_cast<uint64_t>(afeye_s.address()),
+                            v8::afeye::kSrcFingerprint);
+      }
     }
   }
 #endif
diff --git a/v8/src/heap/factory-base.cc b/v8/src/heap/factory-base.cc
index debbaae..41267f0 100644
--- a/v8/src/heap/factory-base.cc
+++ b/v8/src/heap/factory-base.cc
@@ -34,6 +34,9 @@
 #include "src/objects/template-objects-inl.h"
 #include "src/roots/roots.h"
 #include "src/sandbox/check.h"
+#ifdef V8_AFEYE
+#include "src/afeye/taint.h"
+#endif
 
 namespace v8 {
 namespace internal {
@@ -924,6 +927,28 @@ MaybeHandle<SeqTwoByteString> FactoryBase<Impl>::NewRawSharedTwoByteString(
       AllocationType::kSharedOld, AllocationHint());
 }
 
+#ifdef V8_AFEYE
+namespace {
+V8_INLINE uint64_t AfeyeStrKey(Tagged<String> s) {
+  return static_cast<uint64_t>(s.address());
+}
+V8_INLINE void AfeyeTaintConcat(Tagged<String> result, Tagged<String> left,
+                                Tagged<String> right) {
+  if (!v8::afeye::TaintEnabled()) return;
+  v8::afeye::TaintTag t =
+      v8::afeye::TaintGet(v8::afeye::kKeyHeap, AfeyeStrKey(left)) |
+      v8::afeye::TaintGet(v8::afeye::kKeyHeap, AfeyeStrKey(right));
+  if (t != v8::afeye::kSrcNone)
+    v8::afeye::TaintSet(v8::afeye::kKeyHeap, AfeyeStrKey(result), t);
+}
+V8_INLINE void AfeyeTaintCopy(Tagged<String> result, Tagged<String> src) {
+  if (!v8::afeye::TaintEnabled()) return;
+  v8::afeye::TaintPropagate(v8::afeye::kKeyHeap, AfeyeStrKey(result),
+                            v8::afeye::kKeyHeap, AfeyeStrKey(src));
+}
+}
+#endif  // V8_AFEYE
+
 template <typename Impl>
 template <template <typename> typename HandleType>
   requires(std::is_convertible_v<HandleType<String>, DirectHandle<String>>)
@@ -997,6 +1022,9 @@ HandleType<String>::MaybeType FactoryBase<Impl>::NewConsString(
             right->template GetDirectStringChars<uint8_t>(no_gc, access_guard);
         CopyChars(dest + left_length, src, right_length);
       }
+#ifdef V8_AFEYE
+      AfeyeTaintConcat(*result, *left, *right);
+#endif
       return result;
     }
 
@@ -1009,6 +1037,9 @@ HandleType<String>::MaybeType FactoryBase<Impl>::NewConsString(
     String::WriteToFlat(*left, sink, 0, left->length(), access_guard);
     String::WriteToFlat(*right, sink + left->length(), 0, right->length(),
                         access_guard);
+#ifdef V8_AFEYE
+    AfeyeTaintConcat(*result, *left, *right);
+#endif
     return result;
   }
 
@@ -1041,6 +1072,9 @@ Handle<String> FactoryBase<Impl>::NewConsString(DirectHandle<String> left,
   result->set_length(length);
   result->set_first(*left, *mode);
   result->set_second(*right, *mode);
+#ifdef V8_AFEYE
+  AfeyeTaintConcat(result, *left, *right);
+#endif
   return handle(result, isolate());
 }
 
diff --git a/v8/src/heap/factory.cc b/v8/src/heap/factory.cc
index 9716d02..1128ec0 100644
--- a/v8/src/heap/factory.cc
+++ b/v8/src/heap/factory.cc
@@ -3,6 +3,9 @@
 // found in the LICENSE file.
 
 #include "src/heap/factory.h"
+#ifdef V8_AFEYE
+#include "src/afeye/taint.h"
+#endif
 
 #include <algorithm>  // For copy
 #include <memory>     // For shared_ptr<>
@@ -1470,7 +1473,21 @@ Handle<String> Factory::NewProperSubString(DirectHandle<String> str,
   }
 
   if (!v8_flags.string_slices || length < SlicedString::kMinLength) {
+#ifdef V8_AFEYE
+    {
+      Handle<String> afeye_r = NewCopiedSubstring(str, begin, length);
+      if (v8::afeye::TaintEnabled()) {
+        v8::afeye::TaintTag t = v8::afeye::TaintGet(
+            v8::afeye::kKeyHeap, static_cast<uint64_t>(str->address()));
+        if (t != v8::afeye::kSrcNone)
+          v8::afeye::TaintSet(v8::afeye::kKeyHeap,
+                              static_cast<uint64_t>(afeye_r->address()), t);
+      }
+      return afeye_r;
+    }
+#else
     return NewCopiedSubstring(str, begin, length);
+#endif
   }
 
   int offset = begin;
@@ -1496,6 +1513,15 @@ Handle<String> Factory::NewProperSubString(DirectHandle<String> str,
   slice->set_length(length);
   slice->set_parent(*str);
   slice->set_offset(offset);
+#ifdef V8_AFEYE
+  if (v8::afeye::TaintEnabled()) {
+    v8::afeye::TaintTag t = v8::afeye::TaintGet(
+        v8::afeye::kKeyHeap, static_cast<uint64_t>(str->address()));
+    if (t != v8::afeye::kSrcNone)
+      v8::afeye::TaintSet(v8::afeye::kKeyHeap,
+                          static_cast<uint64_t>(slice->address()), t);
+  }
+#endif
   return handle(slice, isolate());
 }
 

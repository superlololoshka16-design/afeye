--- a/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
+++ b/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
@@ -530,6 +530,20 @@ ImageData* BaseRenderingContext2D::getImageDataInternal(
     }
   }
 
+#ifdef BLINK_AFEYE
+  // afeye (0014): the getImageData RESULT. 0007 logs the ask (rect+ctx); this
+  // logs the pixels - the actual canvas-2d fingerprint digest the antifraud
+  // hashes. Head-span only (kSpanCap in the sink bounds it); RawByteSpan is
+  // the same accessor GetSkPixmap uses, so the bytes are the delivered ones.
+  if (blink::afeye::Enabled() && image_data) {
+    base::span<uint8_t> afeye_px = image_data->RawByteSpan();
+    if (!afeye_px.empty()) {
+      blink::afeye::EmitSpan(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                             "canvas/get-image-data-r", afeye_px.data(),
+                             static_cast<uint32_t>(afeye_px.size()));
+    }
+  }
+#endif
   return image_data;
 }
 
--- a/third_party/blink/renderer/modules/webaudio/analyser_node.cc
+++ b/third_party/blink/renderer/modules/webaudio/analyser_node.cc
@@ -25,6 +25,12 @@
 
 #include "third_party/blink/renderer/modules/webaudio/analyser_node.h"
 
+#ifdef BLINK_AFEYE
+#include <atomic>
+
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
+
 #include "third_party/blink/renderer/bindings/modules/v8/v8_analyser_options.h"
 #include "third_party/blink/renderer/modules/webaudio/analyser_handler.h"
 #include "third_party/blink/renderer/modules/webaudio/audio_graph_tracer.h"
@@ -116,22 +122,85 @@ double AnalyserNode::smoothingTimeConstant() const {
   return GetAnalyserHandler().SmoothingTimeConstant();
 }
 
+#ifdef BLINK_AFEYE
+namespace {
+
+// afeye (0014): the realtime audio fingerprint - AnalyserNode getters are the
+// AudioContext-fingerprint surface 0007 missed (it only hooked the OFFLINE
+// render destination). Every read delivers bytes into the caller's typed
+// array; emit a head-span of the filled buffer. One shared cap: an audio
+// cadence loop polls these per frame.
+constexpr uint64_t kAfeyeAnalyserCap = 2000000;
+std::atomic<uint64_t> g_afeye_analyser{0};
+
+void AfeyeEmitAnalyserSpan(const char* tag, DOMUint8Array* array) {
+  if (!array) return;
+  size_t len = array->length();
+  if (len == 0) return;
+  blink::afeye::EmitSpan(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                         tag,
+                         static_cast<const uint8_t*>(
+                             array->BaseAddressMaybeShared()),
+                         static_cast<uint32_t>(len));
+}
+
+void AfeyeEmitAnalyserSpanF32(const char* tag, DOMFloat32Array* array) {
+  if (!array) return;
+  size_t len = array->length();
+  if (len == 0) return;
+  blink::afeye::EmitSpan(
+      blink::afeye::kFingerprint, blink::afeye::NowNs(), tag,
+      reinterpret_cast<const uint8_t*>(array->BaseAddressMaybeShared()),
+      static_cast<uint32_t>(len * sizeof(float)));
+}
+
+}  // namespace
+#endif
+
 void AnalyserNode::getFloatFrequencyData(NotShared<DOMFloat32Array> array) {
   GetAnalyserHandler().GetFloatFrequencyData(array.Get(),
                                              context()->currentTime());
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() &&
+      g_afeye_analyser.fetch_add(1, std::memory_order_relaxed) <
+          kAfeyeAnalyserCap) {
+    AfeyeEmitAnalyserSpanF32("audio/float-frequency", array.Get());
+  }
+#endif
 }
 
 void AnalyserNode::getByteFrequencyData(NotShared<DOMUint8Array> array) {
   GetAnalyserHandler().GetByteFrequencyData(array.Get(),
                                             context()->currentTime());
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() &&
+      g_afeye_analyser.fetch_add(1, std::memory_order_relaxed) <
+          kAfeyeAnalyserCap) {
+    AfeyeEmitAnalyserSpan("audio/byte-frequency", array.Get());
+  }
+#endif
 }
 
 void AnalyserNode::getFloatTimeDomainData(NotShared<DOMFloat32Array> array) {
   GetAnalyserHandler().GetFloatTimeDomainData(array.Get());
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() &&
+      g_afeye_analyser.fetch_add(1, std::memory_order_relaxed) <
+          kAfeyeAnalyserCap) {
+    AfeyeEmitAnalyserSpanF32("audio/float-timedomain", array.Get());
+  }
+#endif
 }
 
 void AnalyserNode::getByteTimeDomainData(NotShared<DOMUint8Array> array) {
   GetAnalyserHandler().GetByteTimeDomainData(array.Get());
+#ifdef BLINK_AFEYE
+  if (blink::afeye::Enabled() &&
+      g_afeye_analyser.fetch_add(1, std::memory_order_relaxed) <
+          kAfeyeAnalyserCap) {
+    AfeyeEmitAnalyserSpan("audio/byte-timedomain", array.Get());
+  }
+#endif
 }
 
 void AnalyserNode::ReportDidCreate() {
--- a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
+++ b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
@@ -5340,6 +5340,23 @@ void WebGLRenderingContextBase::ReadPixelsHelper(GLint x,
                                                  GLenum type,
                                                  DOMArrayBufferView* pixels,
                                                  int64_t offset) {
+#ifdef BLINK_AFEYE
+  // afeye (0014): WebGL readPixels is THE WebGL-fingerprint readback - the
+  // rendered digest FingerprintJS/CreepJS hash. 0007 covered getParameter/
+  // getExtension/getShaderPrecisionFormat but readPixels was invisible: only
+  // the ASK was nameable via kind-29, never the geometry or the pixels. This
+  // logs the ask here (the single funnel WebGL1 and WebGL2 readPixels both
+  // call) and the result head-span below.
+  if (blink::afeye::Enabled()) {
+    char afeye_buf[160];
+    std::snprintf(afeye_buf, sizeof(afeye_buf),
+                  "webgl/read-pixels rect=%d,%d %dx%d fmt=%u type=%u",
+                  x, y, width, height, static_cast<unsigned>(format),
+                  static_cast<unsigned>(type));
+    blink::afeye::EmitStr(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                          afeye_buf);
+  }
+#endif
   if (isContextLost())
     return;
   // Due to WebGL's same-origin restrictions, it is not possible to
@@ -5406,6 +5423,19 @@ void WebGLRenderingContextBase::ReadPixelsHelper(GLint x,
       return;
     }
     ContextGL()->ReadPixels(x, y, width, height, format, type, data);
+#ifdef BLINK_AFEYE
+    // the RESULT: the pixels are now in `data`. Emit a head-span (the digest
+    // signal; the full buffer is caller-owned and can be megabytes). Capped
+    // by the sink span limit; content-slice links this blob to the send that
+    // consumed it. No copy beyond the head-span, no re-render.
+    if (blink::afeye::Enabled() && data && buffer_size.IsValid()) {
+      size_t afeye_n = buffer_size.ValueOrDie();
+      blink::afeye::EmitSpan(blink::afeye::kFingerprint, blink::afeye::NowNs(),
+                             "webgl/read-pixels-r",
+                             static_cast<const uint8_t*>(data),
+                             static_cast<uint32_t>(afeye_n));
+    }
+#endif
   }
 }
 

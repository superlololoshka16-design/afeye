diff --git a/services/network/afeye_sink.h b/services/network/afeye_sink.h
new file mode 100644
index 0000000000..1111111111
--- /dev/null
+++ b/services/network/afeye_sink.h
@@ -0,0 +0,37 @@
+#ifndef SERVICES_NETWORK_PUBLIC_CPP_AFEYE_SINK_H_
+#define SERVICES_NETWORK_PUBLIC_CPP_AFEYE_SINK_H_
+
+#include <stddef.h>
+#include <stdint.h>
+
+#ifdef NET_AFEYE
+
+namespace network {
+namespace afeye {
+
+enum EventKind : uint8_t {
+  kSinkHello = 0,
+  kNetReq = 17,
+  kNetRespBody = 18,
+  kWebSocket = 19,
+};
+
+bool Enabled();
+uint64_t NowNs();
+void EmitFlags(uint8_t kind, uint64_t ts_ns, uint8_t flags, const void* data,
+               uint32_t len);
+void Emit(uint8_t kind, uint64_t ts_ns, const void* data, uint32_t len);
+void EmitStr(uint8_t kind, uint64_t ts_ns, const char* s);
+void EmitTwoStr(uint8_t kind, uint64_t ts_ns, const char* a, const char* b);
+void EmitSpan(uint8_t kind, uint64_t ts_ns, const char* tag,
+              const uint8_t* bytes, uint32_t n);
+uint64_t SinkDropped();
+bool Flush(uint32_t timeout_ms);
+
+}
+}  // namespace network
+
+#endif
+
+#endif
+
+
diff --git a/services/network/afeye_sink.cc b/services/network/afeye_sink.cc
new file mode 100644
index 0000000000..1111111111
--- /dev/null
+++ b/services/network/afeye_sink.cc
@@ -0,0 +37,280 @@
+#include "services/network/afeye_sink.h"
+
+#ifdef NET_AFEYE
+
+#include <atomic>
+#include <cerrno>
+#include <chrono>
+#include <cstddef>
+#include <cstdio>
+#include <cstdlib>
+#include <cstring>
+#include <mutex>
+#include <string>
+#include <thread>
+#include <vector>
+
+#include <fcntl.h>
+#include <sys/stat.h>
+#include <sys/types.h>
+#include <time.h>
+#include <unistd.h>
+
+namespace network {
+namespace afeye {
+namespace {
+
+constexpr size_t kRingBytes = 8u << 20;
+constexpr size_t kMaxRecord = 1u << 20;
+constexpr uint32_t kSpanCap = 256u * 1024u;
+constexpr const char* kLayer = "net";
+
+std::atomic<bool> g_on{false};
+std::atomic<uint64_t> g_dropped{0};
+std::once_flag g_init_once;
+std::atomic<pid_t> g_pid{0};  // v12.4: atomic + CAS - see Enabled()
+
+struct Ring {
+  std::atomic_flag lock = ATOMIC_FLAG_INIT;
+  size_t head = 0;
+  size_t tail = 0;
+  uint8_t data[kRingBytes + kMaxRecord];
+};
+Ring g_ring;
+
+uint64_t MonoNs() {
+  struct timespec ts;
+  clock_gettime(CLOCK_MONOTONIC, &ts);
+  return static_cast<uint64_t>(ts.tv_sec) * 1000000000ull +
+         static_cast<uint64_t>(ts.tv_nsec);
+}
+
+bool EnvOn() {
+  const char* v = ::getenv("AFEYE_SINK");
+  return v != nullptr && *v != '\0' && v[0] != '0';
+}
+
+const char* RawDir() {
+  static const char* kDir = []() -> const char* {
+    const char* v = ::getenv("AFEYE_RAW_DIR");
+    return (v != nullptr && *v != '\0') ? v : "/tmp/afeye-raw";
+  }();
+  return kDir;
+}
+
+// v12.4 (0001 pattern): write errors are COUNTED and folded into the
+// drop witness - a silent return made ENOSPC/EIO invisible loss.
+std::atomic<uint64_t> g_write_err{0};
+
+void WriteAll(int fd, const uint8_t* p, size_t n) {
+  size_t off = 0;
+  while (off < n) {
+    ssize_t w = ::write(fd, p + off, n - off);
+    if (w < 0) {
+      if (errno == EINTR) continue;
+      g_write_err.fetch_add(1, std::memory_order_relaxed);
+      return;
+    }
+    if (w == 0) return;
+    off += static_cast<size_t>(w);
+  }
+}
+
+void EmitHello(int fd) {
+  char msg[96];
+  int n = std::snprintf(msg, sizeof(msg), "afeye-sink/%s v2 pid=%d", kLayer,
+                        static_cast<int>(::getpid()));
+  if (n <= 0) return;
+  uint8_t rec[16 + 96];
+  uint32_t total = 16 + static_cast<uint32_t>(n);
+  std::memcpy(rec, &total, sizeof(total));
+  rec[4] = 0;
+  rec[5] = 0;
+  rec[6] = 0;
+  rec[7] = 0;
+  uint64_t ts = MonoNs();
+  std::memcpy(rec + 8, &ts, sizeof(ts));
+  std::memcpy(rec + 16, msg, static_cast<size_t>(n));
+  WriteAll(fd, rec, total);
+}
+
+void DrainLoop() {
+  char pid[16];
+  std::snprintf(pid, sizeof(pid), "%d", static_cast<int>(::getpid()));
+  std::string path = std::string(RawDir()) + "/" + kLayer + "-" + pid + ".rec";
+  int fd = ::open(path.c_str(), O_CREAT | O_APPEND | O_WRONLY, 0666);
+  if (fd >= 0) EmitHello(fd);
+  // v12.4 (N6, 0001 pattern): pre-sized buffer, memcpy under lock, write
+  // outside it - buf.assign() malloc'd up to 1 MiB INSIDE the ring lock.
+  static thread_local std::vector<uint8_t> buf;
+  buf.resize(kMaxRecord);
+  for (;;) {
+    size_t len = 0;
+    bool have = false;
+    uint32_t spin = 0;
+    while (g_ring.lock.test_and_set(std::memory_order_acquire)) {
+      if (++spin >= 64) {
+        spin = 0;
+        std::this_thread::yield();
+      }
+    }
+    if (g_ring.tail != g_ring.head) {
+      uint32_t len32;
+      std::memcpy(&len32, g_ring.data + g_ring.tail, sizeof(len32));
+      len = len32;
+      if (len < 16 || len > kMaxRecord) {
+        g_ring.tail = g_ring.head;
+      } else {
+        std::memcpy(buf.data(), g_ring.data + g_ring.tail, len);
+        size_t nt = g_ring.tail + len;
+        if (nt >= kRingBytes) nt -= kRingBytes;
+        g_ring.tail = nt;
+        have = true;
+      }
+    }
+    g_ring.lock.clear(std::memory_order_release);
+    uint64_t werr_before = g_write_err.load(std::memory_order_relaxed);
+    if (have) {
+      if (fd >= 0) {
+        WriteAll(fd, buf.data(), len);
+        // v12.4 (0001 pattern): failed write -> force reopen next cycle.
+        if (g_write_err.load(std::memory_order_relaxed) != werr_before) {
+          ::close(fd);
+          fd = -1;
+        }
+      }
+      if (fd < 0) {
+        fd = ::open(path.c_str(), O_CREAT | O_APPEND | O_WRONLY, 0666);
+        if (fd >= 0) EmitHello(fd);
+      }
+    } else {
+      std::this_thread::sleep_for(std::chrono::microseconds(500));
+    }
+  }
+}
+
+// v12.1: fork safety (see 0001 for the rationale). The network service
+// utility process also forks (e.g. crashpad helpers); same re-init rule.
+void ReinitAfterFork() {
+  // v12.4 (0001 pattern hardened): clear the lock unconditionally - the
+  // dead parent thread may have held it at fork time and test_and_set
+  // would spin forever. Race-free: in the fresh child only the calling
+  // thread exists.
+  g_ring.lock.clear(std::memory_order_release);
+  g_ring.tail = 0;
+  g_ring.head = 0;
+  g_dropped.store(0, std::memory_order_relaxed);
+  g_pid.store(::getpid(), std::memory_order_release);
+  std::thread(DrainLoop).detach();
+}
+
+void InitOnce() {
+  if (!EnvOn()) return;
+  ::mkdir(RawDir(), 0777);
+  ::chmod(RawDir(), 0777);
+  g_on.store(true, std::memory_order_release);
+  g_pid.store(::getpid(), std::memory_order_release);
+  std::thread(DrainLoop).detach();
+  std::atexit([] { Flush(500); });
+}
+
+}  // namespace
+
+bool Enabled() {
+  std::call_once(g_init_once, InitOnce);
+  // v12.4: CAS so concurrent child threads admit exactly ONE reinit (two
+  // drain threads would interleave WriteAll chunks into one file); a
+  // disabled sink never spawns a drain thread or a .rec file (N4).
+  pid_t me = ::getpid();
+  if (g_pid.load(std::memory_order_acquire) != me) {
+    pid_t old = g_pid.load(std::memory_order_acquire);
+    if (g_pid.compare_exchange_strong(old, me, std::memory_order_acq_rel)) {
+      ReinitAfterFork();
+    }
+  }
+  return g_on.load(std::memory_order_acquire);
+}
+
+uint64_t NowNs() { return MonoNs(); }
+
+void EmitFlags(uint8_t kind, uint64_t ts_ns, uint8_t flags, const void* data,
+               uint32_t len) {
+  if (!g_on.load(std::memory_order_acquire)) return;
+  if (data == nullptr || len == 0) return;
+  if (len > kMaxRecord - 16) {
+    len = kMaxRecord - 16;
+    flags |= 1;
+  }
+  const size_t need = 16 + static_cast<size_t>(len);
+  while (g_ring.lock.test_and_set(std::memory_order_acquire)) {
+  }
+  const size_t h = g_ring.head;
+  const size_t t = g_ring.tail;
+  const size_t used = (h >= t) ? h - t : kRingBytes - t + h;
+  if (kRingBytes - used < need + 8) {
+    g_ring.lock.clear(std::memory_order_release);
+    g_dropped.fetch_add(1, std::memory_order_relaxed);
+    return;
+  }
+  uint8_t* rec = g_ring.data + h;
+  const uint32_t total = static_cast<uint32_t>(need);
+  std::memcpy(rec, &total, sizeof(total));
+  rec[4] = kind;
+  rec[5] = flags;
+  rec[6] = 0;
+  rec[7] = 0;
+  std::memcpy(rec + 8, &ts_ns, sizeof(ts_ns));
+  std::memcpy(rec + 16, data, len);
+  size_t nh = h + need;
+  if (nh >= kRingBytes) nh -= kRingBytes;
+  g_ring.head = nh;
+  g_ring.lock.clear(std::memory_order_release);
+}
+
+void Emit(uint8_t kind, uint64_t ts_ns, const void* data, uint32_t len) {
+  EmitFlags(kind, ts_ns, 0, data, len);
+}
+
+void EmitStr(uint8_t kind, uint64_t ts_ns, const char* s) {
+  if (s == nullptr) return;
+  EmitFlags(kind, ts_ns, 0, s, static_cast<uint32_t>(std::strlen(s)));
+}
+
+void EmitTwoStr(uint8_t kind, uint64_t ts_ns, const char* a, const char* b) {
+  if (a == nullptr || b == nullptr) return;
+  size_t la = std::strlen(a);
+  size_t lb = std::strlen(b);
+  bool trunc = false;
+  const size_t kCap = kMaxRecord - 17;
+  if (la > kCap) {
+    la = kCap;
+    trunc = true;
+  }
+  if (lb > kCap - la) {
+    lb = kCap - la;
+    trunc = true;
+  }
+  static thread_local std::string buf;
+  buf.clear();
+  buf.reserve(la + lb + 1);
+  buf.append(a, la);
+  buf.push_back('\0');
+  buf.append(b, lb);
+  EmitFlags(kind, ts_ns, trunc ? 1 : 0, buf.data(),
+            static_cast<uint32_t>(buf.size()));
+}
+
+void EmitSpan(uint8_t kind, uint64_t ts_ns, const char* tag,
+              const uint8_t* bytes, uint32_t n) {
+  if (tag == nullptr || bytes == nullptr || n == 0) return;
+  size_t lt = std::strlen(tag);
+  if (lt > 64) lt = 64;
+  bool trunc = false;
+  if (n > kSpanCap) {
+    n = kSpanCap;
+    trunc = true;
+  }
+  static thread_local std::vector<char> buf;
+  buf.clear();
+  buf.reserve(lt + 1 + n);
+  buf.insert(buf.end(), tag, tag + lt);
+  buf.push_back('\0');
+  buf.insert(buf.end(), reinterpret_cast<const char*>(bytes),
+             reinterpret_cast<const char*>(bytes) + n);
+  EmitFlags(kind, ts_ns, trunc ? 1 : 0, buf.data(),
+            static_cast<uint32_t>(buf.size()));
+}
+
+// v12.4 (0001 pattern): ring drops AND write errors - see SinkDropped.
+uint64_t SinkDropped() {
+  return g_dropped.load(std::memory_order_relaxed) +
+         g_write_err.load(std::memory_order_relaxed);
+}
+
+bool Flush(uint32_t timeout_ms) {
+  if (!g_on.load(std::memory_order_acquire)) return true;
+  auto deadline = std::chrono::steady_clock::now() +
+                  std::chrono::milliseconds(timeout_ms);
+  while (std::chrono::steady_clock::now() < deadline) {
+    bool empty;
+    while (g_ring.lock.test_and_set(std::memory_order_acquire)) {
+    }
+    empty = g_ring.tail == g_ring.head;
+    g_ring.lock.clear(std::memory_order_release);
+    if (empty) return true;
+    std::this_thread::sleep_for(std::chrono::microseconds(200));
+  }
+  return false;
+}
+
+}
+}  // namespace network
+
+#endif
+
+
--- a/services/network/BUILD.gn
+++ b/services/network/BUILD.gn
@@ -8,6 +325,11 @@
 import("//net/features.gni")
 import("//services/network/public/cpp/features.gni")
 import("//testing/libfuzzer/fuzzer_test.gni")
+
+declare_args() {
+  # afeye: env-activated record sink compiled into the network service.
+  network_enable_afeye = false
+}
 
 component("network_service") {
   sources = [
@@ -447,6 +769,13 @@
   }
 
   defines = [ "IS_NETWORK_SERVICE_IMPL" ]
+  if (network_enable_afeye) {
+    sources += [
+      "afeye_sink.cc",
+      "afeye_sink.h",
+    ]
+    defines += [ "NET_AFEYE" ]
+  }
 }
 
 if (is_linux || is_chromeos) {

--- a/services/network/url_loader.cc
+++ b/services/network/url_loader.cc
@@ -3,6 +332,10 @@
 // found in the LICENSE file.
 
 #include "services/network/url_loader.h"
+#ifdef NET_AFEYE
+#include <string>
+#include "services/network/afeye_sink.h"
+#endif
 
 #include <algorithm>
 #include <limits>
@@ -633,6 +966,31 @@
 void URLLoader::SetUpUpload(
     const ResourceRequest& request,
     base::expected<std::vector<base::File>, net::Error> file_open_result) {
+#ifdef NET_AFEYE
+  if (network::afeye::Enabled() && request.request_body) {
+    uint64_t afeye_total = 0;
+    for (const auto& afeye_el : *request.request_body->elements()) {
+      if (afeye_el.type() != network::DataElement::Tag::kBytes) {
+        continue;
+      }
+      const auto& afeye_bytes = afeye_el.As<network::DataElementBytes>().bytes();
+      afeye_total += afeye_bytes.size();
+      if (!afeye_bytes.empty()) {
+        network::afeye::EmitSpan(network::afeye::kNetReq,
+                                 network::afeye::NowNs(), "req-body",
+                                 afeye_bytes.data(),
+                                 static_cast<uint32_t>(afeye_bytes.size()));
+      }
+    }
+    if (afeye_total) {
+      char afeye_sum[80];
+      std::snprintf(afeye_sum, sizeof(afeye_sum), "req-body-total=%llu",
+                    static_cast<unsigned long long>(afeye_total));
+      network::afeye::EmitStr(network::afeye::kNetReq,
+                             network::afeye::NowNs(), afeye_sum);
+    }
+  }
+#endif
   if (file_opener_for_upload_) {
     file_opener_for_upload_.reset();
     // This corresponds to the LogBlockedBy call made just before creating
@@ -739,6 +1097,20 @@
 }
 
 void URLLoader::ScheduleStart() {
+#ifdef NET_AFEYE
+  if (network::afeye::Enabled() && url_request_) {
+    network::afeye::EmitTwoStr(network::afeye::kNetReq,
+                               network::afeye::NowNs(),
+                               url_request_->method().c_str(),
+                               url_request_->url().spec().c_str());
+    std::string afeye_headers = url_request_->extra_request_headers().ToString();
+    if (!afeye_headers.empty()) {
+      network::afeye::EmitTwoStr(network::afeye::kNetReq,
+                                 network::afeye::NowNs(), "req-headers",
+                                 afeye_headers.c_str());
+    }
+  }
+#endif
   TRACE_EVENT("loading", "URLLoader::ScheduleStart",
               net::NetLogWithSourceToFlow(url_request_->net_log()));
   bool defer = false;
@@ -1195,6 +1567,28 @@
 }
 
 void URLLoader::ContinueOnResponseStarted() {
+#ifdef NET_AFEYE
+  if (network::afeye::Enabled() && url_request_) {
+    const net::HttpResponseInfo& afeye_info = url_request_->response_info();
+    if (afeye_info.headers) {
+      std::string afeye_raw = afeye_info.headers->raw_headers();
+      if (!afeye_raw.empty()) {
+        network::afeye::EmitTwoStr(network::afeye::kNetRespBody,
+                                   network::afeye::NowNs(),
+                                   "resp-headers", afeye_raw.c_str());
+      }
+      std::string afeye_mime;
+      if (afeye_info.headers->GetMimeType(&afeye_mime)) {
+        char afeye_code[96];
+        std::snprintf(afeye_code, sizeof(afeye_code),
+                      "resp-mime=%s code=%d", afeye_mime.c_str(),
+                      afeye_info.headers->response_code());
+        network::afeye::EmitStr(network::afeye::kNetRespBody,
+                                 network::afeye::NowNs(), afeye_code);
+      }
+    }
+  }
+#endif
   // Do not account header bytes when reporting received body bytes to client.
   reported_total_encoded_bytes_ =
       url_request_->GetTotalReceivedBytes().InBytes();
@@ -1647,6 +2041,18 @@
   url_read_state_ = URLReadState::kWaitMojoPipeWritable;
 
   size_t new_data_offset = pending_write_buffer_offset_;
+#ifdef NET_AFEYE
+  if (network::afeye::Enabled() && pending_write_ && num_bytes > 0 &&
+      !into_slop_bucket) {
+    const uint8_t* afeye_p = reinterpret_cast<const uint8_t*>(
+        pending_write_->buffer()) + new_data_offset;
+    network::afeye::EmitSpan(network::afeye::kNetRespBody,
+                             network::afeye::NowNs(), "resp-body",
+                             afeye_p,
+                             static_cast<uint32_t>(
+                                 num_bytes > 0x40000 ? 0x40000 : num_bytes));
+  }
+#endif
   MaybeCollectDurableMessage(new_data_offset, num_bytes);
 
   if (num_bytes > 0) {
@@ -1941,6 +2347,21 @@
 }
 
 void URLLoader::NotifyCompleted(int error_code) {
+#ifdef NET_AFEYE
+  if (network::afeye::Enabled() && url_request_) {
+    char afeye_fin[512];
+    int afeye_n = std::snprintf(afeye_fin, sizeof(afeye_fin),
+                               "complete url=%s err=%d recv=%lld",
+                               url_request_->url().spec().c_str(),
+                               error_code,
+                               static_cast<long long>(
+                                   url_request_->GetTotalReceivedBytes().InBytes()));
+    if (afeye_n > 0) {
+      network::afeye::EmitStr(network::afeye::kNetRespBody,
+                             network::afeye::NowNs(), afeye_fin);
+    }
+  }
+#endif
   // Ensure sending the final upload progress message here, since
   // OnResponseCompleted can be called without OnResponseStarted on cancellation
   // or error cases.

--- a/services/network/websocket.cc
+++ b/services/network/websocket.cc
@@ -3,6 +424,9 @@
 // found in the LICENSE file.
 
 #include "services/network/websocket.h"
+#ifdef NET_AFEYE
+#include "services/network/afeye_sink.h"
+#endif
 
 #include <inttypes.h>
 #include <string.h>
@@ -411,6 +835,16 @@
     bool fin,
     net::WebSocketFrameHeader::OpCode type,
     base::span<const char> payload) {
+#ifdef NET_AFEYE
+  if (network::afeye::Enabled() && payload.size() > 0) {
+    network::afeye::EmitSpan(network::afeye::kWebSocket,
+                             network::afeye::NowNs(),
+                             fin ? "ws-frame-fin" : "ws-frame",
+                             reinterpret_cast<const uint8_t*>(payload.data()),
+                             static_cast<uint32_t>(
+                                 payload.size() > 0x40000 ? 0x40000                                                         : payload.size()));
+  }
+#endif
   DVLOG(3) << "WebSocketEventHandler::OnDataFrame @"
            << reinterpret_cast<void*>(this) << " fin=" << fin
            << " type=" << type << " data is " << payload.size() << " bytes";

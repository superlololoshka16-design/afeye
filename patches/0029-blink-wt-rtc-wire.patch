diff --git a/third_party/blink/renderer/modules/peerconnection/rtc_data_channel.cc b/third_party/blink/renderer/modules/peerconnection/rtc_data_channel.cc
index 62e191a..6455c48 100644
--- a/third_party/blink/renderer/modules/peerconnection/rtc_data_channel.cc
+++ b/third_party/blink/renderer/modules/peerconnection/rtc_data_channel.cc
@@ -24,6 +24,13 @@
 
 #include "third_party/blink/renderer/modules/peerconnection/rtc_data_channel.h"
 
+#ifdef BLINK_AFEYE
+#include <atomic>
+#include <cstdlib>
+
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
+
 #include <limits>
 #include <memory>
 #include <string>
@@ -67,6 +74,25 @@
 
 namespace blink {
 
+#ifdef BLINK_AFEYE
+namespace {
+std::atomic<uint64_t> g_afeye_rtc{0};
+inline bool AfeyeWireOn() {
+  static const bool off = []() {
+    const char* v = ::getenv("AFEYE_TRACE_WT_RTC");
+    return v != nullptr && *v != '\0' && v[0] == '0';
+  }();
+  return !off &&
+         g_afeye_rtc.fetch_add(1, std::memory_order_relaxed) < 200000;
+}
+inline void AfeyeEmitWire(const char* tag, const uint8_t* data, size_t len) {
+  if (len == 0 || !AfeyeWireOn()) return;
+  afeye::EmitSpan(afeye::kWebSocket, afeye::NowNs(), tag, data,
+                  static_cast<uint32_t>(len));
+}
+}
+#endif  // BLINK_AFEYE
+
 namespace {
 
 // These values are persisted to logs. Entries should not be renumbered and
@@ -757,6 +783,12 @@ void RTCDataChannel::OnMaxMessageSizeChange(int max_message_size) {
 
 void RTCDataChannel::OnMessage(webrtc::DataBuffer buffer) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
+#ifdef BLINK_AFEYE
+  {
+    base::span<const uint8_t> afeye_span(buffer.data);
+    AfeyeEmitWire("rtc-datachannel-in", afeye_span.data(), afeye_span.size());
+  }
+#endif
 
   if (buffer.binary) {
     switch (binary_type_) {
@@ -824,6 +856,12 @@ void RTCDataChannel::SendRawData(const char* data, size_t length) {
 void RTCDataChannel::SendDataBuffer(webrtc::DataBuffer data_buffer) {
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
   CHECK(!was_transferred_);
+#ifdef BLINK_AFEYE
+  {
+    base::span<const uint8_t> afeye_span(data_buffer.data);
+    AfeyeEmitWire("rtc-datachannel-out", afeye_span.data(), afeye_span.size());
+  }
+#endif
 
   // SCTP data channels queue the packet on failure and always return true, so
   // Send can be called asynchronously for them.
diff --git a/third_party/blink/renderer/modules/webtransport/incoming_stream.cc b/third_party/blink/renderer/modules/webtransport/incoming_stream.cc
index 0e8a03c..f8c772c 100644
--- a/third_party/blink/renderer/modules/webtransport/incoming_stream.cc
+++ b/third_party/blink/renderer/modules/webtransport/incoming_stream.cc
@@ -4,6 +4,13 @@
 
 #include "third_party/blink/renderer/modules/webtransport/incoming_stream.h"
 
+#ifdef BLINK_AFEYE
+#include <atomic>
+#include <cstdlib>
+
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
+
 #include <string.h>
 
 #include <utility>
@@ -35,6 +42,25 @@
 
 namespace blink {
 
+#ifdef BLINK_AFEYE
+namespace {
+std::atomic<uint64_t> g_afeye_wtin{0};
+inline bool AfeyeWireOn() {
+  static const bool off = []() {
+    const char* v = ::getenv("AFEYE_TRACE_WT_RTC");
+    return v != nullptr && *v != '\0' && v[0] == '0';
+  }();
+  return !off &&
+         g_afeye_wtin.fetch_add(1, std::memory_order_relaxed) < 200000;
+}
+inline void AfeyeEmitWire(const char* tag, const uint8_t* data, size_t len) {
+  if (len == 0 || !AfeyeWireOn()) return;
+  afeye::EmitSpan(afeye::kWebSocket, afeye::NowNs(), tag, data,
+                  static_cast<uint32_t>(len));
+}
+}
+#endif  // BLINK_AFEYE
+
 // An implementation of UnderlyingByteSourceBase that forwards all operations to
 // the IncomingStream object that created it.
 class IncomingStream::UnderlyingByteSource final
@@ -234,6 +260,10 @@ void IncomingStream::ReadFromPipeAndEnqueue(ExceptionState& exception_state) {
     case MOJO_RESULT_OK: {
       in_two_phase_read_ = true;
 
+#ifdef BLINK_AFEYE
+      AfeyeEmitWire("wt-stream-in", buffer.data(), buffer.size());
+#endif
+
       // RespondBYOBRequestOrEnqueueBytes() may re-enter this method via pull().
       size_t read_bytes =
           RespondBYOBRequestOrEnqueueBytes(buffer, exception_state);
diff --git a/third_party/blink/renderer/modules/webtransport/outgoing_stream.cc b/third_party/blink/renderer/modules/webtransport/outgoing_stream.cc
index f95d6a4..c27e645 100644
--- a/third_party/blink/renderer/modules/webtransport/outgoing_stream.cc
+++ b/third_party/blink/renderer/modules/webtransport/outgoing_stream.cc
@@ -4,6 +4,13 @@
 
 #include "third_party/blink/renderer/modules/webtransport/outgoing_stream.h"
 
+#ifdef BLINK_AFEYE
+#include <atomic>
+#include <cstdlib>
+
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
+
 #include <cstring>
 #include <utility>
 
@@ -36,6 +43,25 @@
 
 namespace blink {
 
+#ifdef BLINK_AFEYE
+namespace {
+std::atomic<uint64_t> g_afeye_wtout{0};
+inline bool AfeyeWireOn() {
+  static const bool off = []() {
+    const char* v = ::getenv("AFEYE_TRACE_WT_RTC");
+    return v != nullptr && *v != '\0' && v[0] == '0';
+  }();
+  return !off &&
+         g_afeye_wtout.fetch_add(1, std::memory_order_relaxed) < 200000;
+}
+inline void AfeyeEmitWire(const char* tag, const uint8_t* data, size_t len) {
+  if (len == 0 || !AfeyeWireOn()) return;
+  afeye::EmitSpan(afeye::kWebSocket, afeye::NowNs(), tag, data,
+                  static_cast<uint32_t>(len));
+}
+}
+#endif  // BLINK_AFEYE
+
 namespace {
 
 class SendStreamAbortAlgorithm final : public AbortSignal::Algorithm {
@@ -437,6 +463,10 @@ size_t OutgoingStream::WriteDataSynchronously(base::span<const uint8_t> data) {
            << data.size() << ")";
   DCHECK(data_pipe_);
 
+#ifdef BLINK_AFEYE
+  AfeyeEmitWire("wt-stream-out", data.data(), data.size());
+#endif
+
   size_t actually_written_bytes = 0;
   MojoResult result = data_pipe_->WriteData(data, MOJO_WRITE_DATA_FLAG_NONE,
                                             actually_written_bytes);
diff --git a/third_party/blink/renderer/modules/webtransport/web_transport.cc b/third_party/blink/renderer/modules/webtransport/web_transport.cc
index 066fa6d..2ad6dd1 100644
--- a/third_party/blink/renderer/modules/webtransport/web_transport.cc
+++ b/third_party/blink/renderer/modules/webtransport/web_transport.cc
@@ -4,6 +4,13 @@
 
 #include "third_party/blink/renderer/modules/webtransport/web_transport.h"
 
+#ifdef BLINK_AFEYE
+#include <atomic>
+#include <cstdlib>
+
+#include "third_party/blink/renderer/platform/afeye/sink.h"
+#endif
+
 #include <stdint.h>
 
 #include <limits>
@@ -84,6 +91,25 @@
 
 namespace blink {
 
+#ifdef BLINK_AFEYE
+namespace {
+std::atomic<uint64_t> g_afeye_wtdg{0};
+inline bool AfeyeWireOn() {
+  static const bool off = []() {
+    const char* v = ::getenv("AFEYE_TRACE_WT_RTC");
+    return v != nullptr && *v != '\0' && v[0] == '0';
+  }();
+  return !off &&
+         g_afeye_wtdg.fetch_add(1, std::memory_order_relaxed) < 200000;
+}
+inline void AfeyeEmitWire(const char* tag, const uint8_t* data, size_t len) {
+  if (len == 0 || !AfeyeWireOn()) return;
+  afeye::EmitSpan(afeye::kWebSocket, afeye::NowNs(), tag, data,
+                  static_cast<uint32_t>(len));
+}
+}
+#endif  // BLINK_AFEYE
+
 namespace {
 
 // The incoming max age to to be used when datagrams.incomingMaxAge is set to
@@ -256,6 +282,9 @@ class WebTransport::DatagramUnderlyingSink final : public UnderlyingSinkBase {
 
  private:
   ScriptPromise<IDLUndefined> SendDatagram(base::span<const uint8_t> data) {
+#ifdef BLINK_AFEYE
+    AfeyeEmitWire("wt-datagram-out", data.data(), data.size());
+#endif
     auto* resolver = MakeGarbageCollected<ScriptPromiseResolver<IDLUndefined>>(
         web_transport_->script_state_);
     pending_datagrams_resolvers_.push_back(resolver);
@@ -1194,6 +1223,9 @@ void WebTransport::OnHandshakeFailed(
 }
 
 void WebTransport::OnDatagramReceived(base::span<const uint8_t> data) {
+#ifdef BLINK_AFEYE
+  AfeyeEmitWire("wt-datagram-in", data.data(), data.size());
+#endif
   datagram_underlying_source_->OnDatagramReceived(
       received_datagrams_controller_, data);
 }
